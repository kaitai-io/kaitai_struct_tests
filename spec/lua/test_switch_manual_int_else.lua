-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("switch_manual_int_else")

TestSwitchManualIntElse = {}

function TestSwitchManualIntElse:test_switch_manual_int_else()
    local r = SwitchManualIntElse:from_file("src/switch_opcodes2.bin")

    luaunit.assertEquals(#r.opcodes, 4)
    luaunit.assertEquals(r.opcodes[0 + 1].code, 83)
    luaunit.assertEquals(r.opcodes[0 + 1].body.value, "foo")
    luaunit.assertEquals(r.opcodes[1 + 1].code, 88)
    luaunit.assertEquals(r.opcodes[1 + 1].body.filler, 66)
    luaunit.assertEquals(r.opcodes[2 + 1].code, 89)
    luaunit.assertEquals(r.opcodes[2 + 1].body.filler, 51966)
    luaunit.assertEquals(r.opcodes[3 + 1].code, 73)
    luaunit.assertEquals(r.opcodes[3 + 1].body.value, 7)
end
