-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("cast_nested")

TestCastNested = {}

function TestCastNested:test_cast_nested()
    local r = CastNested:from_file("src/switch_opcodes.bin")

    luaunit.assertEquals(r.opcodes_0_str.value, "foobar")
    luaunit.assertEquals(r.opcodes_0_str_value, "foobar")
    luaunit.assertEquals(r.opcodes_1_int.value, 66)
    luaunit.assertEquals(r.opcodes_1_int_value, 66)
end
