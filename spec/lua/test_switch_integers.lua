-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("switch_integers")

TestSwitchIntegers = {}

function TestSwitchIntegers:test_switch_integers()
    local r = SwitchIntegers:from_file("src/switch_integers.bin")

    luaunit.assertEquals(#r.opcodes, 4)
    luaunit.assertEquals(r.opcodes[0 + 1].code, 1)
    luaunit.assertEquals(r.opcodes[0 + 1].body, 7)
    luaunit.assertEquals(r.opcodes[1 + 1].code, 2)
    luaunit.assertEquals(r.opcodes[1 + 1].body, 16448)
    luaunit.assertEquals(r.opcodes[2 + 1].code, 4)
    luaunit.assertEquals(r.opcodes[2 + 1].body, 4919)
    luaunit.assertEquals(r.opcodes[3 + 1].code, 8)
    luaunit.assertEquals(r.opcodes[3 + 1].body, 4919)
end
