-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("repeat_until_sized")

TestRepeatUntilSized = {}

function TestRepeatUntilSized:test_repeat_until_sized()
    local r = RepeatUntilSized:from_file("src/repeat_until_process.bin")

    luaunit.assertEquals(#r.records, 3)
    luaunit.assertEquals(r.records[0 + 1].marker, 232)
    luaunit.assertEquals(r.records[0 + 1].body, 2863311546)
    luaunit.assertEquals(r.records[1 + 1].marker, 250)
    luaunit.assertEquals(r.records[1 + 1].body, 2863315102)
    luaunit.assertEquals(r.records[2 + 1].marker, 170)
    luaunit.assertEquals(r.records[2 + 1].body, 1431655765)
end
