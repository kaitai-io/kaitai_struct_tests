-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("combine_str")

TestCombineStr = {}

function TestCombineStr:test_combine_str()
    local r = CombineStr:from_file("src/term_strz.bin")

    luaunit.assertEquals(r.str_term, "foo")
    luaunit.assertEquals(r.str_limit, "bar|")
    luaunit.assertEquals(r.str_eos, "baz@")
    luaunit.assertEquals(r.str_calc, "bar")
    luaunit.assertEquals(r.str_calc_bytes, "baz")
    luaunit.assertEquals(r.term_or_limit, "foo")
    luaunit.assertEquals(r.term_or_eos, "baz@")
    luaunit.assertEquals(r.term_or_calc, "foo")
    luaunit.assertEquals(r.term_or_calc_bytes, "baz")
    luaunit.assertEquals(r.limit_or_eos, "bar|")
    luaunit.assertEquals(r.limit_or_calc, "bar")
    luaunit.assertEquals(r.limit_or_calc_bytes, "bar|")
    luaunit.assertEquals(r.eos_or_calc, "bar")
    luaunit.assertEquals(r.eos_or_calc_bytes, "baz@")
    luaunit.assertEquals(r.calc_or_calc_bytes, "baz")
end
