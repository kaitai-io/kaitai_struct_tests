-- Autogenerated from KST: please remove this line if doing any edits by hand!

local luaunit = require("luaunit")

require("nav_parent3")

TestNavParent3 = {}

function TestNavParent3:test_nav_parent3()
    local r = NavParent3:from_file("src/nav_parent2.bin")

    luaunit.assertEquals(r.ofs_tags, 8)
    luaunit.assertEquals(r.num_tags, 2)
    luaunit.assertEquals(r.tags[0 + 1].name, "RAHC")
    luaunit.assertEquals(r.tags[0 + 1].ofs, 32)
    luaunit.assertEquals(r.tags[0 + 1].num_items, 3)
    luaunit.assertEquals(r.tags[0 + 1].tag_content.content, "foo")
    luaunit.assertEquals(r.tags[1 + 1].name, "RAHC")
    luaunit.assertEquals(r.tags[1 + 1].ofs, 35)
    luaunit.assertEquals(r.tags[1 + 1].num_items, 6)
    luaunit.assertEquals(r.tags[1 + 1].tag_content.content, "barbaz")
end
