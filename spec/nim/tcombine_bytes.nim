# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/combine_bytes
import ../test_utils

let r = CombineBytes.fromFile("src/term_strz.bin")

test "CombineBytes":

  check(r.bytesTerm == string(@[102, 111, 111].mapIt(it.toByte).toString))
  check(r.bytesLimit == string(@[98, 97, 114, 124].mapIt(it.toByte).toString))
  check(r.bytesEos == string(@[98, 97, 122, 64].mapIt(it.toByte).toString))
  check(r.bytesCalc == string(@[82, 110, 68].mapIt(it.toByte).toString))
  check(r.termOrLimit == string(@[102, 111, 111].mapIt(it.toByte).toString))
  check(r.termOrEos == string(@[98, 97, 122, 64].mapIt(it.toByte).toString))
  check(r.termOrCalc == string(@[102, 111, 111].mapIt(it.toByte).toString))
  check(r.limitOrEos == string(@[98, 97, 114, 124].mapIt(it.toByte).toString))
  check(r.limitOrCalc == string(@[82, 110, 68].mapIt(it.toByte).toString))
  check(r.eosOrCalc == string(@[98, 97, 122, 64].mapIt(it.toByte).toString))
  discard
