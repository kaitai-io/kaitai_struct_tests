# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/params_pass_array_int
import ../test_utils

let r = ParamsPassArrayInt.fromFile("src/position_to_end.bin")

test "ParamsPassArrayInt":

  check(len(r.passInts.nums) == int(3))
  check(r.passInts.nums[0] == uint16(513))
  check(r.passInts.nums[1] == uint16(1027))
  check(r.passInts.nums[2] == uint16(1541))
  check(len(r.passIntsCalc.nums) == int(2))
  check(r.passIntsCalc.nums[0] == uint16(27643))
  check(r.passIntsCalc.nums[1] == uint16(7))
  discard
