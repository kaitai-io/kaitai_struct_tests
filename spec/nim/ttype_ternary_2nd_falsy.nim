# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/type_ternary_2nd_falsy
import ../test_utils

let r = TypeTernary2ndFalsy.fromFile("src/switch_integers.bin")

test "TypeTernary2ndFalsy":

  check(r.vFalse == bool(false))
  check(r.vIntZero == int8(0))
  check(r.vIntNegZero == int(-0))
  check(r.vFloatZero == float64(0.0))
  check(r.vFloatNegZero == float64(-0.0))
  check(r.vStrWZero == string("0"))
  check(len(r.vStrWZero) == int(1))
  check(r.ut.m == uint8(7))
  check(r.vNullUt == nil)
  check(r.vStrEmpty == string(""))
  check(len(r.vStrEmpty) == int(0))
  check(len(r.intArray) == int(2))
  check(len(r.vIntArrayEmpty) == int(0))
  discard
