# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/params_pass_struct
import ../test_utils

let r = ParamsPassStruct.fromFile("src/enum_negative.bin")

test "ParamsPassStruct":

  check(r.first.foo == uint8(255))
  check(r.one.bar.qux == uint8(1))
  check((ParamsPassStruct_Block(r.one.foo)).foo == uint8(255))
  check((ParamsPassStruct_Block(r.one.bar.foo)).foo == uint8(255))
  discard
