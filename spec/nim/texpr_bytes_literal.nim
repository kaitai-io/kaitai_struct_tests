# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/expr_bytes_literal
import ../test_utils

let r = ExprBytesLiteral.fromFile("src/enum_negative.bin")

test "ExprBytesLiteral":

  check(len(r.literal) == int(2))
  check(r.literal[0] == uint8(-1))
  check(r.literal[1] == uint8(1))
  discard
