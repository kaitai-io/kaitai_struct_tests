# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/switch_else_only

var
  stream = newFileStream("test_out/nim/junitreports/switch_else_only.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: SwitchElseOnly":
    let r = SwitchElseOnly.fromFile("src/switch_opcodes.bin")

    check(r.opcode == int8(83))
    check(r.primByte == int8(102))
    check(r.struct.value == seq[byte](@[111, 111, 98, 97].mapIt(toByte(it))))
    check(r.structSized.value == seq[byte](@[114, 0, 73, 66].mapIt(toByte(it))))

close(outputFormatter)
