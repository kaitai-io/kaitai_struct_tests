# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/expr_ops_parens

var
  stream = newFileStream("test_out/nim/junitreports/expr_ops_parens.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: ExprOpsParens":
    let r = ExprOpsParens.fromFile("src/enum_negative.bin")

    check(r.iSumToStr == string("29"))
    check(r.strConcatLen == int(10))
    check(r.strConcatRev == string("9876543210"))
    check(r.strConcatSubstr2To7 == string("23456"))
    check(r.strConcatToI == int(123456789))
    check(r.boolEq == int(0))
    check(r.boolAnd == int(0))
    check(r.boolOr == int(1))

close(outputFormatter)
