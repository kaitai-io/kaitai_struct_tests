# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/switch_integers2

var
  stream = newFileStream("test_out/nim/junitreports/switch_integers2.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: SwitchIntegers2":
    let r = SwitchIntegers2.fromFile("src/switch_integers.bin")

    check(r.code == uint8(1))
    check(r.len == uint64(7))
    check(r.ham == seq[byte](@[2, 64, 64, 4, 55, 19, 0].mapIt(toByte(it))))
    check(r.padding == uint8(0))
    check(r.lenModStr == string("13"))

close(outputFormatter)
