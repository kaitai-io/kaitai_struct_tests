# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils
import ../../../compiled/nim/repeat_until_sized
import ../test_utils

let r = RepeatUntilSized.fromFile("src/repeat_until_process.bin")

doAssert(len(r.records) == int(3))
doAssert(r.records[0].marker == uint8(232))
doAssert(r.records[0].body == uint32(2863311546'u32))
doAssert(r.records[1].marker == uint8(250))
doAssert(r.records[1].body == uint32(2863315102'u32))
doAssert(r.records[2].marker == uint8(170))
doAssert(r.records[2].body == uint32(1431655765))
