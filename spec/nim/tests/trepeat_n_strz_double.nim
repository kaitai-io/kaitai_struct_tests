# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, ../testhelpers
import ../../../compiled/nim/repeat_n_strz_double

var
  stream = newFileStream("test_out/nim/junitreports/repeat_n_strz_double.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: RepeatNStrzDouble":
    let r = RepeatNStrzDouble.fromFile("src/repeat_n_strz.bin")

    check(r.qty == uint32(2))
    check(r.lines1 == seq[string](@["foo"]))
    check(r.lines2 == seq[string](@["bar"]))

close(outputFormatter)
