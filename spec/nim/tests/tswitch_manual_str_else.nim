# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/switch_manual_str_else
import ../test_utils

let r = SwitchManualStrElse.fromFile("src/switch_opcodes2.bin")

test "SwitchManualStrElse":

  check(len(r.opcodes) == int(4))
  check(r.opcodes[0].code == string("S"))
  check((SwitchManualStrElse_Opcode_Strval(r.opcodes[0].body)).value == string("foo"))
  check(r.opcodes[1].code == string("X"))
  check((SwitchManualStrElse_Opcode_Noneval(r.opcodes[1].body)).filler == uint32(66))
  check(r.opcodes[2].code == string("Y"))
  check((SwitchManualStrElse_Opcode_Noneval(r.opcodes[2].body)).filler == uint32(51966))
  check(r.opcodes[3].code == string("I"))
  check((SwitchManualStrElse_Opcode_Intval(r.opcodes[3].body)).value == uint8(7))
  discard
