# Autogenerated from KST: please remove this line if doing any edits by hand!

import ../../../compiled/nim/imports_circular_b
import os, streams, options, sequtils, unittest
import ../../../compiled/nim/imports_circular_a
import ../test_utils

let r = ImportsCircularA.fromFile("src/fixed_struct.bin")

test "ImportsCircularA":

  check(r.code == uint8(80))
  check(r.two.initial == uint8(65))
  check(r.two.backRef.code == uint8(67))
  check(r.two.backRef.two.initial == uint8(75))
  check(r.two.backRef.two.backRef == nil)
  discard
