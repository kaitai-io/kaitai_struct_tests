# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/bytes_pad_term

var
  stream = newFileStream("test_out/nim/junitreports/bytes_pad_term.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: BytesPadTerm":
    let r = BytesPadTerm.fromFile("src/str_pad_term.bin")

    check(r.strPad == seq[byte](@[115, 116, 114, 49].mapIt(toByte(it))))
    check(r.strTerm == seq[byte](@[115, 116, 114, 50, 102, 111, 111].mapIt(toByte(it))))
    check(r.strTermAndPad == seq[byte](@[115, 116, 114, 43, 43, 43, 51, 98, 97, 114, 43, 43, 43].mapIt(toByte(it))))
    check(r.strTermInclude == seq[byte](@[115, 116, 114, 52, 98, 97, 122, 64].mapIt(toByte(it))))

close(outputFormatter)
