# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/enum_if

var
  stream = newFileStream("test_out/nim/junitreports/enum_if.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: EnumIf":
    let r = EnumIf.fromFile("src/if_struct.bin")

    check(r.op1.opcode == Opcodes())
    check(r.op1.argStr.str == string("foo"))
    check(r.op2.opcode == Opcodes())
    check(r.op2.argTuple.num1 == uint8(66))
    check(r.op2.argTuple.num2 == uint8(67))
    check(r.op3.opcode == Opcodes())
    check(r.op3.argStr.str == string("bar"))

close(outputFormatter)
