# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/combine_bytes
import ../test_utils

let r = CombineBytes.fromFile("src/term_strz.bin")

test "CombineBytes":

  check(r.bytesTerm == seq[byte](@[102'u8, 111, 111]))
  check(r.bytesLimit == seq[byte](@[98'u8, 97, 114, 124]))
  check(r.bytesEos == seq[byte](@[98'u8, 97, 122, 64]))
  check(r.bytesCalc == seq[byte](@[82'u8, 110, 68]))
  check(r.termOrLimit == seq[byte](@[102'u8, 111, 111]))
  check(r.termOrEos == seq[byte](@[98'u8, 97, 122, 64]))
  check(r.termOrCalc == seq[byte](@[102'u8, 111, 111]))
  check(r.limitOrEos == seq[byte](@[98'u8, 97, 114, 124]))
  check(r.limitOrCalc == seq[byte](@[82'u8, 110, 68]))
  check(r.eosOrCalc == seq[byte](@[98'u8, 97, 122, 64]))
  discard
