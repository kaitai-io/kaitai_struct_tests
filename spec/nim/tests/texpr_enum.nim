# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/expr_enum
import ../test_utils

let r = ExprEnum.fromFile("src/term_strz.bin")

test "ExprEnum":

  check(r.constDog == expr_enum.dog)
  check(r.derivedBoom == expr_enum.boom)
  check(r.derivedDog == expr_enum.dog)
  discard
