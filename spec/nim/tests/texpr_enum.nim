# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/expr_enum

var
  stream = newFileStream("test_out/nim/junitreports/expr_enum.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: ExprEnum":
    let r = ExprEnum.fromFile("src/term_strz.bin")

    check(r.constDog == Animal())
    check(r.derivedBoom == Animal())
    check(r.derivedDog == Animal())

close(outputFormatter)
