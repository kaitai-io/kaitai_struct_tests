# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/process_rotate

var
  stream = newFileStream("test_out/nim/junitreports/process_rotate.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: ProcessRotate":
    let r = ProcessRotate.fromFile("src/process_rotate.bin")

    check(r.buf1 == seq[byte](@[72, 101, 108, 108, 111].mapIt(toByte(it))))
    check(r.buf2 == seq[byte](@[87, 111, 114, 108, 100].mapIt(toByte(it))))
    check(r.buf3 == seq[byte](@[84, 104, 101, 114, 101].mapIt(toByte(it))))

close(outputFormatter)
