# Autogenerated from KST: please remove this line if doing any edits by hand!

import os, streams, options, sequtils, unittest
import ../../../compiled/nim/switch_manual_int_size
import ../test_utils

let r = SwitchManualIntSize.fromFile("src/switch_tlv.bin")

test "SwitchManualIntSize":

  check(len(r.chunks) == int(4))
  check(r.chunks[0].code == uint8(17))
  check((SwitchManualIntSize_Chunk_ChunkMeta(r.chunks[0].body)).title == string("Stuff"))
  check((SwitchManualIntSize_Chunk_ChunkMeta(r.chunks[0].body)).author == string("Me"))
  check(r.chunks[1].code == uint8(34))
  check((SwitchManualIntSize_Chunk_ChunkDir(r.chunks[1].body)).entries == @[string("AAAA"), "BBBB", "CCCC"])
  check(r.chunks[2].code == uint8(51))
  check((seq[byte](r.chunks[2].body)) == @[16'u8, 32'u8, 48'u8, 64'u8, 80'u8, 96'u8, 112'u8, -128'u8])
  check(r.chunks[3].code == uint8(255))
  check((seq[byte](r.chunks[3].body)) == @[])
  discard
