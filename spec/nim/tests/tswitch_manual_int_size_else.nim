# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, ../testhelpers
import ../../../compiled/nim/switch_manual_int_size_else

var
  stream = newFileStream("test_out/nim/junitreports/switch_manual_int_size_else.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: SwitchManualIntSizeElse":
    let r = SwitchManualIntSizeElse.fromFile("src/switch_tlv.bin")

    check(len(r.chunks) == 4)
    check(r.chunks[0].code == 17)
    check(r.chunks[0].body.title == "Stuff")
    check(r.chunks[0].body.author == "Me")
    check(r.chunks[1].code == 34)
    check(r.chunks[1].body.entries == @["AAAA", "BBBB", "CCCC"])
    check(r.chunks[2].code == 51)
    check(r.chunks[2].body.rest == @[16, 32, 48, 64, 80, 96, 112, -128])
    check(r.chunks[3].code == 255)
    check(r.chunks[3].body.rest == @[])

close(outputFormatter)
