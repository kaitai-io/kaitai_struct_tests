# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, ../testhelpers
import ../../../compiled/nim/instance_user_array

var
  stream = newFileStream("test_out/nim/junitreports/instance_user_array.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: InstanceUserArray":
    let r = InstanceUserArray.fromFile("src/instance_std_array.bin")

    check(r.ofs == 16)
    check(r.qtyEntries == 3)
    check(r.entrySize == 4)
    check(len(r.userEntries) == 3)
    check(r.userEntries[0].word1 == 4369)
    check(r.userEntries[0].word2 == 4369)
    check(r.userEntries[1].word1 == 8738)
    check(r.userEntries[1].word2 == 8738)
    check(r.userEntries[2].word1 == 13107)
    check(r.userEntries[2].word2 == 13107)

close(outputFormatter)
