# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, ../testhelpers
import ../../../compiled/nim/nested_types3

var
  stream = newFileStream("test_out/nim/junitreports/nested_types3.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: NestedTypes3":
    let r = NestedTypes3.fromFile("src/fixed_struct.bin")

    check(r.aCc.valueCc == 80)
    check(r.aCD.valueD == 65)
    check(r.b.valueB == 67)
    check(r.b.aCc.valueCc == 75)
    check(r.b.aCD.valueD == 45)

close(outputFormatter)
