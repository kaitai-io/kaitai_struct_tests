# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/process_custom

var
  stream = newFileStream("test_out/nim/junitreports/process_custom.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: ProcessCustom":
    let r = ProcessCustom.fromFile("src/process_rotate.bin")

    check(r.buf1 == seq[byte](@[16, -77, -108, -108, -12].mapIt(toByte(it))))
    check(r.buf2 == seq[byte](@[95, -70, 123, -109, 99, 35, 95].mapIt(toByte(it))))
    check(r.buf3 == seq[byte](@[41, 51, -79, 56, -79].mapIt(toByte(it))))

close(outputFormatter)
