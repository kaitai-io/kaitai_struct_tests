# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, ../testhelpers
import ../../../compiled/nim/expr_3

var
  stream = newFileStream("test_out/nim/junitreports/expr_3.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: Expr3":
    let r = Expr3.fromFile("src/fixed_struct.bin")

    check(r.one == uint8(80))
    check(r.two == string("ACK"))
    check(r.three == string("@ACK"))
    check(r.four == string("_ACK_"))
    check(r.isStrEq == bool(true))
    check(r.isStrNe == bool(false))
    check(r.isStrLt == bool(true))
    check(r.isStrGt == bool(false))
    check(r.isStrLe == bool(true))
    check(r.isStrGe == bool(false))
    check(r.isStrLt2 == bool(true))
    check(r.testNot == bool(true))

close(outputFormatter)
