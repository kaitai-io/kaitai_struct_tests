# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest, os, streams, options, sequtils, ../testhelpers
import ../../../compiled/nim/switch_bytearray

var
  stream = newFileStream("test_out/nim/junitreports/switch_bytearray.xml", fmWrite)
  outputFormatter = newJUnitOutputFormatter(stream)
addOutputFormatter(outputFormatter)

suite "Kaitai Struct Compiler Test Suite":
  test "Nim: SwitchBytearray":
    let r = SwitchBytearray.fromFile("src/switch_opcodes.bin")

    check(len(r.opcodes) == int(4))
    check(r.opcodes[0].code == seq[byte](@[83].mapIt(toByte(it))))
    check(r.opcodes[0].body.value == string("foobar"))
    check(r.opcodes[1].code == seq[byte](@[73].mapIt(toByte(it))))
    check(r.opcodes[1].body.value == uint8(66))
    check(r.opcodes[2].code == seq[byte](@[73].mapIt(toByte(it))))
    check(r.opcodes[2].body.value == uint8(55))
    check(r.opcodes[3].code == seq[byte](@[83].mapIt(toByte(it))))
    check(r.opcodes[3].body.value == string(""))

close(outputFormatter)
