// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "process_coerce_switch.h"
#include <iostream>
#include <fstream>
#include <vector>

BOOST_AUTO_TEST_CASE(test_process_coerce_switch) {
    std::ifstream ifs("src/process_coerce_switch.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    process_coerce_switch_t* r = new process_coerce_switch_t(&ks);

    BOOST_CHECK_EQUAL(r->buf_type(), 0);
    BOOST_CHECK_EQUAL(r->flag(), 0);
    BOOST_CHECK_EQUAL(static_cast<process_coerce_switch_t::foo_t*>(r->buf())->bar(), std::string("\x41\x41\x41\x41", 4));

    delete r;
}
