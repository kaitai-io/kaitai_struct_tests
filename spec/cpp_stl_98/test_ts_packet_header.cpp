// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "ts_packet_header.h"
#include <iostream>
#include <fstream>
#include <vector>

BOOST_AUTO_TEST_CASE(test_ts_packet_header) {
    std::ifstream ifs("src/ts_packet.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    ts_packet_header_t* r = new ts_packet_header_t(&ks);

    BOOST_CHECK_EQUAL(r->sync_byte(), 71);
    BOOST_CHECK_EQUAL(r->transport_error_indicator(), false);
    BOOST_CHECK_EQUAL(r->payload_unit_start_indicator(), false);
    BOOST_CHECK_EQUAL(r->transport_priority(), true);
    BOOST_CHECK_EQUAL(r->pid(), 33);
    BOOST_CHECK_EQUAL(r->transport_scrambling_control(), 0);
    BOOST_CHECK_EQUAL(r->adaptation_field_control(), ts_packet_header_t::ADAPTATION_FIELD_CONTROL_ENUM_PAYLOAD_ONLY);

    delete r;
}
