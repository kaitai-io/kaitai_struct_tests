// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "repeat_n_struct.h"
#include <iostream>
#include <fstream>
#include <vector>

BOOST_AUTO_TEST_CASE(test_repeat_n_struct) {
    std::ifstream ifs("src/repeat_n_struct.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    repeat_n_struct_t* r = new repeat_n_struct_t(&ks);

    BOOST_CHECK_EQUAL(r->chunks()->size(), 2);
    BOOST_CHECK_EQUAL(r->chunks()->at(0)->offset(), 16);
    BOOST_CHECK_EQUAL(r->chunks()->at(0)->len(), 8312);
    BOOST_CHECK_EQUAL(r->chunks()->at(1)->offset(), 8328);
    BOOST_CHECK_EQUAL(r->chunks()->at(1)->len(), 15);

    delete r;
}
