# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest
from reserved_python_keywords import ReservedPythonKeywords
import reserved_python_keywords

class TestReservedPythonKeywords(unittest.TestCase):
    def test_reserved_python_keywords(self):
        with ReservedPythonKeywords.from_file('src/full256.bin') as r:
            self.assertEqual(r.and_, 0)
            self.assertEqual(r.as_, 1)
            self.assertEqual(r.assert_, 2)
            self.assertEqual(r.async_, 3)
            self.assertEqual(r.await_, 4)
            self.assertEqual(r.break_, 5)
            self.assertEqual(r.class_, 6)
            self.assertEqual(r.continue_, 7)
            self.assertEqual(r.def_, 8)
            self.assertEqual(r.del_, 9)
            self.assertEqual(r.elif_, 10)
            self.assertEqual(r.else_, 11)
            self.assertEqual(r.except_, 12)
            self.assertEqual(r.false, 13)
            self.assertEqual(r.finally_, 14)
            self.assertEqual(r.for_, 15)
            self.assertEqual(r.from_, 16)
            self.assertEqual(r.global_, 17)
            self.assertEqual(r.if_, 18)
            self.assertEqual(r.import_, 19)
            self.assertEqual(r.in_, 20)
            self.assertEqual(r.is_, 21)
            self.assertEqual(r.lambda_, 22)
            self.assertEqual(r.none, 23)
            self.assertEqual(r.nonlocal_, 24)
            self.assertEqual(r.not_, 25)
            self.assertEqual(r.or_, 26)
            self.assertEqual(r.pass_, 27)
            self.assertEqual(r.raise_, 28)
            self.assertEqual(r.return_, 29)
            self.assertEqual(r.true, 30)
            self.assertEqual(r.try_, 31)
            self.assertEqual(r.while_, 32)
            self.assertEqual(r.with_, 33)
            self.assertEqual(r.yield_, 34)
            self.assertEqual(r.keyword_struct_def.while_, 35)
            self.assertEqual(r.keyword_struct_def.for_, 36)
            self.assertEqual(r.keyword_struct_def.with_, 37)
            self.assertEqual(r.keyword_struct_false.pass_, 38)
            self.assertEqual(r.keyword_enum, reserved_python_keywords.ReservedPythonKeywords.True_.and_)
            self.assertEqual(r.keyword_nested_enum, reserved_python_keywords.ReservedPythonKeywords.Def.Try.except_)
            self.assertEqual(r.inst_keyword_seq, 1)
            self.assertEqual(r.inst_keyword_enum, reserved_python_keywords.ReservedPythonKeywords.True_.if_)
            self.assertEqual(r.inst_keyword_enum_to_i, 6)
            self.assertEqual(r.keyword_struct_def.return_, 71)
