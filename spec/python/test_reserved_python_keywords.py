# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest
from reserved_python_keywords import ReservedPythonKeywords
import reserved_python_keywords

class TestReservedPythonKeywords(unittest.TestCase):
    def test_reserved_python_keywords(self):
        with ReservedPythonKeywords.from_file('src/full256.bin') as r:
            self.assertEqual(r.and, 1)
            self.assertEqual(r.as, 2)
            self.assertEqual(r.assert, 3)
            self.assertEqual(r.async, 4)
            self.assertEqual(r.await, 5)
            self.assertEqual(r.break, 6)
            self.assertEqual(r.class, 7)
            self.assertEqual(r.continue, 8)
            self.assertEqual(r.def, 9)
            self.assertEqual(r.del, 10)
            self.assertEqual(r.elif, 11)
            self.assertEqual(r.else, 12)
            self.assertEqual(r.except, 13)
            self.assertEqual(r.false, 14)
            self.assertEqual(r.finally, 15)
            self.assertEqual(r.for, 16)
            self.assertEqual(r.from, 17)
            self.assertEqual(r.global, 18)
            self.assertEqual(r.if, 19)
            self.assertEqual(r.import, 20)
            self.assertEqual(r.in, 21)
            self.assertEqual(r.is, 22)
            self.assertEqual(r.lambda, 23)
            self.assertEqual(r.none, 24)
            self.assertEqual(r.nonlocal, 25)
            self.assertEqual(r.not, 26)
            self.assertEqual(r.or, 27)
            self.assertEqual(r.pass, 28)
            self.assertEqual(r.raise, 29)
            self.assertEqual(r.return, 30)
            self.assertEqual(r.true, 31)
            self.assertEqual(r.try, 32)
            self.assertEqual(r.while, 33)
            self.assertEqual(r.with, 34)
            self.assertEqual(r.yield, 35)
            self.assertEqual(r.keyword_struct.while, 36)
            self.assertEqual(r.keyword_struct.for, 37)
            self.assertEqual(r.keyword_struct.with, 38)
            self.assertEqual(r.keyword_enum, reserved_python_keywords.ReservedPythonKeywords.True.and)
            self.assertEqual(r.keyword_nested_enum, reserved_python_keywords.ReservedPythonKeywords.Def.Try.except)
            self.assertEqual(r.inst1, 3)
            self.assertEqual(r.keyword_struct.return, 73)
