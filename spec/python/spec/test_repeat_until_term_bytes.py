# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest

from testformats.repeat_until_term_bytes import RepeatUntilTermBytes

class TestRepeatUntilTermBytes(unittest.TestCase):
    def test_repeat_until_term_bytes(self):
        with RepeatUntilTermBytes.from_file('src/repeat_until_process.bin') as r:

            self.assertEqual(len(r.records1), 2)
            self.assertEqual(r.records1[0], b"\xE8\xBA")
            self.assertEqual(r.records1[1], b"")
            self.assertEqual(len(r.records2), 2)
            self.assertEqual(r.records2[0], b"\xAA")
            self.assertEqual(r.records2[1], b"\xFA\x9E\xB8\xAA")
            self.assertEqual(len(r.records3), 2)
            self.assertEqual(r.records3[0], b"\xAA\xAA")
            self.assertEqual(r.records3[1], b"")
