# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest
from testformats.switch_manual_str import SwitchManualStr

class TestSwitchManualStr(unittest.TestCase):
    def test_switch_manual_str(self):
        with SwitchManualStr.from_file('src/switch_opcodes.bin') as r:
            self.assertEqual(len(r.opcodes), 4)
            self.assertEqual(r.opcodes[0].code, u"S")
            self.assertEqual(r.opcodes[0].body.value, u"foobar")
            self.assertEqual(r.opcodes[1].code, u"I")
            self.assertEqual(r.opcodes[1].body.value, 66)
            self.assertEqual(r.opcodes[2].code, u"I")
            self.assertEqual(r.opcodes[2].body.value, 55)
            self.assertEqual(r.opcodes[3].code, u"S")
            self.assertEqual(r.opcodes[3].body.value, u"")
