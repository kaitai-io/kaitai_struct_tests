# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest
from testformats.instance_user_array import InstanceUserArray

class TestInstanceUserArray(unittest.TestCase):
    def test_instance_user_array(self):
        with InstanceUserArray.from_file('src/instance_std_array.bin') as r:
            self.assertEqual(r.ofs, 16)
            self.assertEqual(r.qty_entries, 3)
            self.assertEqual(r.entry_size, 4)
            self.assertEqual(len(r.user_entries), 3)
            self.assertEqual(r.user_entries[0].word1, 4369)
            self.assertEqual(r.user_entries[0].word2, 4369)
            self.assertEqual(r.user_entries[1].word1, 8738)
            self.assertEqual(r.user_entries[1].word2, 8738)
            self.assertEqual(r.user_entries[2].word1, 13107)
            self.assertEqual(r.user_entries[2].word2, 13107)
