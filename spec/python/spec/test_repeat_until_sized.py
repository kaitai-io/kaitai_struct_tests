# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest
from testformats.repeat_until_sized import RepeatUntilSized

class TestRepeatUntilSized(unittest.TestCase):
    def test_repeat_until_sized(self):
        with RepeatUntilSized.from_file('src/repeat_until_process.bin') as r:
            self.assertEqual(len(r.records), 3)
            self.assertEqual(r.records[0].marker, 232)
            self.assertEqual(r.records[0].body, 2863311546)
            self.assertEqual(r.records[1].marker, 250)
            self.assertEqual(r.records[1].body, 2863315102)
            self.assertEqual(r.records[2].marker, 170)
            self.assertEqual(r.records[2].body, 1431655765)
