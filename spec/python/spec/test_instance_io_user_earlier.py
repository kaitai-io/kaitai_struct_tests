# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest

from testformats.instance_io_user_earlier import InstanceIoUserEarlier

class TestInstanceIoUserEarlier(unittest.TestCase):
    def test_instance_io_user_earlier(self):
        with InstanceIoUserEarlier.from_file('src/switch_opcodes2.bin') as r:

            self.assertEqual(r.sized_a.content, 83)
            self.assertEqual(r.into_a.inst.content, 102)
            self.assertEqual(r.a_mid, 28527)
            self.assertEqual(r.into_a.inst.last, 0)
            self.assertEqual(r.sized_a.last, 88)
            self.assertEqual(r.sized_b.content, 66)
            self.assertEqual(r.into_b.inst.content, 0)
            self.assertEqual(r.b_mid, 0)
            self.assertEqual(r.into_b.inst.last, 89)
            self.assertEqual(r.sized_b.last, 254)
            self.assertEqual(r.into_b.indicator, 202)
            self.assertEqual(r.into_a_skipped.indicator, 0)
            self.assertEqual(r.into_a_skipped.inst._io.size(), 0)
            self.assertEqual(r.into_a.indicator, 0)
            self.assertEqual(r.into_a.bar, 73)
            self.assertEqual(r.last_accessor.v, 7)
