# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestBufferedStruct;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use BufferedStruct;

sub test_buffered_struct: Test(7) {
    my $r = BufferedStruct->from_file('src/buffered_struct.bin');

    is($r->len1(), 16, 'Equals');
    is($r->block1()->number1(), 66, 'Equals');
    is($r->block1()->number2(), 67, 'Equals');
    is($r->len2(), 8, 'Equals');
    is($r->block2()->number1(), 68, 'Equals');
    is($r->block2()->number2(), 69, 'Equals');
    is($r->finisher(), 238, 'Equals');
}

Test::Class->runtests;
