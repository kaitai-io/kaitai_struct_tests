# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestExprIoPos;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use ExprIoPos;

sub test_expr_io_pos: Test(6) {
    my $r = ExprIoPos->from_file('src/expr_io_pos.bin');

    is($r->substream1()->my_str(), "CURIOSITY", 'Equals');
    is($r->substream1()->body(), pack('C*', (17, 34, 51, 68)), 'Equals');
    is($r->substream1()->number(), 66, 'Equals');
    is($r->substream2()->my_str(), "KILLED", 'Equals');
    is($r->substream2()->body(), pack('C*', (97, 32, 99, 97, 116)), 'Equals');
    is($r->substream2()->number(), 103, 'Equals');
}

Test::Class->runtests;
