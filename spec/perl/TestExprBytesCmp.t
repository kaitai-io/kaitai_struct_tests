# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestExprBytesCmp;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use ExprBytesCmp;

sub test_expr_bytes_cmp: Test(10) {
    my $r = ExprBytesCmp->from_file('src/fixed_struct.bin');

    is($r->one(), pack('C*', (80)), 'Equals');
    is($r->two(), pack('C*', (65, 67, 75)), 'Equals');
    cmp_ok($r->is_eq(), '==', 1, 'Equals');
    cmp_ok($r->is_ne(), '==', 0, 'Equals');
    cmp_ok($r->is_lt(), '==', 1, 'Equals');
    cmp_ok($r->is_gt(), '==', 0, 'Equals');
    cmp_ok($r->is_le(), '==', 1, 'Equals');
    cmp_ok($r->is_ge(), '==', 0, 'Equals');
    cmp_ok($r->is_lt2(), '==', 0, 'Equals');
    cmp_ok($r->is_gt2(), '==', 1, 'Equals');
}

Test::Class->runtests;
