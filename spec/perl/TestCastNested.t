# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestCastNested;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use CastNested;

sub test_cast_nested: Test(4) {
    my $r = CastNested->from_file('src/switch_opcodes.bin');

    is($r->opcodes_0_str()->value(), "foobar", 'Equals');
    is($r->opcodes_0_str_value(), "foobar", 'Equals');
    is($r->opcodes_1_int()->value(), 66, 'Equals');
    is($r->opcodes_1_int_value(), 66, 'Equals');
}

Test::Class->runtests;
