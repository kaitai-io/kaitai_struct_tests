# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestFloatingPoints;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use FloatingPoints;

sub test_floating_points: Test(8) {
    my $r = FloatingPoints->from_file('src/floating_points.bin');

    ok(abs($r->single_value() - 0.5) < 1e-6, 'Approx equals');
    ok(abs($r->single_value_be() - 0.5) < 1e-6, 'Approx equals');
    ok(abs($r->double_value() - 0.25) < 1e-6, 'Approx equals');
    ok(abs($r->double_value_be() - 0.25) < 1e-6, 'Approx equals');
    ok(abs($r->approximate_value() - 1.2345) < 1e-6, 'Approx equals');
    ok(abs($r->single_value_plus_int() - 1.5) < 1e-6, 'Approx equals');
    ok(abs($r->single_value_plus_float() - 1.0) < 1e-6, 'Approx equals');
    ok(abs($r->double_value_plus_float() - 0.3) < 1e-6, 'Approx equals');
}

Test::Class->runtests;
