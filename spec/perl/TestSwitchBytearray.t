# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestSwitchBytearray;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use SwitchBytearray;

sub test_switch_bytearray: Test(9) {
    my $r = SwitchBytearray->from_file('src/switch_opcodes.bin');

    is(scalar(@{$r->opcodes()}), 4, 'Equals');
    is(@{$r->opcodes()}[0]->code(), pack('C*', (83)), 'Equals');
    is(@{$r->opcodes()}[0]->body()->value(), "foobar", 'Equals');
    is(@{$r->opcodes()}[1]->code(), pack('C*', (73)), 'Equals');
    is(@{$r->opcodes()}[1]->body()->value(), 66, 'Equals');
    is(@{$r->opcodes()}[2]->code(), pack('C*', (73)), 'Equals');
    is(@{$r->opcodes()}[2]->body()->value(), 55, 'Equals');
    is(@{$r->opcodes()}[3]->code(), pack('C*', (83)), 'Equals');
    is(@{$r->opcodes()}[3]->body()->value(), "", 'Equals');
}

Test::Class->runtests;
