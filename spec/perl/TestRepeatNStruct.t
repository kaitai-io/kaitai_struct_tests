# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestRepeatNStruct;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use RepeatNStruct;

sub test_repeat_n_struct: Test(5) {
    my $r = RepeatNStruct->from_file('src/repeat_n_struct.bin');

    is(scalar(@{$r->chunks()}), 2, 'Equals');
    is(@{$r->chunks()}[0]->offset(), 16, 'Equals');
    is(@{$r->chunks()}[0]->len(), 8312, 'Equals');
    is(@{$r->chunks()}[1]->offset(), 8328, 'Equals');
    is(@{$r->chunks()}[1]->len(), 15, 'Equals');
}

Test::Class->runtests;
