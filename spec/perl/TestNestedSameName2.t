# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestNestedSameName2;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use NestedSameName2;

sub test_nested_same_name2: Test(5) {
    my $r = NestedSameName2->from_file('src/nested_same_name2.bin');

    is($r->version(), 66, 'Equals');
    is($r->main_data()->main_size(), 2, 'Equals');
    is($r->main_data()->foo()->data1(), pack('C*', (17, 17, 17, 17)), 'Equals');
    is($r->dummy()->dummy_size(), 3, 'Equals');
    is($r->dummy()->foo()->data2(), pack('C*', (34, 34, 34, 34, 34, 34)), 'Equals');
}

Test::Class->runtests;
