# Autogenerated from KST: please remove this line if doing any edits by hand!

package spec::perl::TestCombineStr;

use strict;
use warnings;
use base qw(Test::Class);
use Test::More;
use CombineStr;

sub test_combine_str: Test(15) {
    my $r = CombineStr->from_file('src/term_strz.bin');

    is($r->str_term(), "foo", 'Equals');
    is($r->str_limit(), "bar|", 'Equals');
    is($r->str_eos(), "baz\@", 'Equals');
    is($r->str_calc(), "bar", 'Equals');
    is($r->str_calc_bytes(), "baz", 'Equals');
    is($r->term_or_limit(), "foo", 'Equals');
    is($r->term_or_eos(), "baz\@", 'Equals');
    is($r->term_or_calc(), "foo", 'Equals');
    is($r->term_or_calc_bytes(), "baz", 'Equals');
    is($r->limit_or_eos(), "bar|", 'Equals');
    is($r->limit_or_calc(), "bar", 'Equals');
    is($r->limit_or_calc_bytes(), "bar|", 'Equals');
    is($r->eos_or_calc(), "bar", 'Equals');
    is($r->eos_or_calc_bytes(), "baz\@", 'Equals');
    is($r->calc_or_calc_bytes(), "baz", 'Equals');
}

Test::Class->runtests;
