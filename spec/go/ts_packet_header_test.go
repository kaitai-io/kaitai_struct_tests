// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestTsPacketHeader(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/ts_packet.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r TsPacketHeader
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}

	assert.EqualValues(t, 71, r.SyncByte)
	assert.EqualValues(t, false, r.TransportErrorIndicator)
	assert.EqualValues(t, false, r.PayloadUnitStartIndicator)
	assert.EqualValues(t, true, r.TransportPriority)
	assert.EqualValues(t, 33, r.Pid)
	assert.EqualValues(t, 0, r.TransportScramblingControl)
	assert.EqualValues(t, TsPacketHeader_AdaptationFieldControlEnum__PayloadOnly, r.AdaptationFieldControl)
}
