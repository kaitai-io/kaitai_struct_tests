// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestReservedPythonKeywords(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/full256.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r ReservedPythonKeywords
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 0, r.And)
	assert.EqualValues(t, 1, r.As)
	assert.EqualValues(t, 2, r.Assert)
	assert.EqualValues(t, 3, r.Async)
	assert.EqualValues(t, 4, r.Await)
	assert.EqualValues(t, 5, r.Break)
	assert.EqualValues(t, 6, r.Class)
	assert.EqualValues(t, 7, r.Continue)
	assert.EqualValues(t, 8, r.Def)
	assert.EqualValues(t, 9, r.Del)
	assert.EqualValues(t, 10, r.Elif)
	assert.EqualValues(t, 11, r.Else)
	assert.EqualValues(t, 12, r.Except)
	assert.EqualValues(t, 13, r.False)
	assert.EqualValues(t, 14, r.Finally)
	assert.EqualValues(t, 15, r.For)
	assert.EqualValues(t, 16, r.From)
	assert.EqualValues(t, 17, r.Global)
	assert.EqualValues(t, 18, r.If)
	assert.EqualValues(t, 19, r.Import)
	assert.EqualValues(t, 20, r.In)
	assert.EqualValues(t, 21, r.Is)
	assert.EqualValues(t, 22, r.Lambda)
	assert.EqualValues(t, 23, r.None)
	assert.EqualValues(t, 24, r.Nonlocal)
	assert.EqualValues(t, 25, r.Not)
	assert.EqualValues(t, 26, r.Or)
	assert.EqualValues(t, 27, r.Pass)
	assert.EqualValues(t, 28, r.Raise)
	assert.EqualValues(t, 29, r.Return)
	assert.EqualValues(t, 30, r.True)
	assert.EqualValues(t, 31, r.Try)
	assert.EqualValues(t, 32, r.While)
	assert.EqualValues(t, 33, r.With)
	assert.EqualValues(t, 34, r.Yield)
	assert.EqualValues(t, 35, r.KeywordStructDef.While)
	assert.EqualValues(t, 36, r.KeywordStructDef.For)
	assert.EqualValues(t, 37, r.KeywordStructDef.With)
	assert.EqualValues(t, 38, r.KeywordStructFalse.Pass)
	assert.EqualValues(t, ReservedPythonKeywords_True__And, r.KeywordEnum)
	assert.EqualValues(t, ReservedPythonKeywords_Def_Try__Except, r.KeywordNestedEnum)
	tmp1, err := r.InstKeywordSeq()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 1, tmp1)
	tmp2, err := r.InstKeywordEnum()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, ReservedPythonKeywords_True__If, tmp2)
	tmp3, err := r.InstKeywordEnumToI()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 6, tmp3)
	tmp4, err := r.KeywordStructDef.Return()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 71, tmp4)
}
