// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestBitsByteAligned(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/fixed_struct.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r BitsByteAligned
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 20, r.One)
	assert.EqualValues(t, 65, r.Byte1)
	assert.EqualValues(t, 2, r.Two)
	assert.EqualValues(t, false, r.Three)
	assert.EqualValues(t, []uint8{75}, r.Byte2)
	assert.EqualValues(t, 2892, r.Four)
	assert.EqualValues(t, 524282, r.Byte3.Inner)
	assert.EqualValues(t, 65, r.FullByte)
	assert.EqualValues(t, 67, r.Byte4)
	assert.EqualValues(t, 1231701, r.Five)
	assert.EqualValues(t, []uint8{45, 68, 69}, r.BytesTerm)
	assert.EqualValues(t, 70, r.Six)
}
