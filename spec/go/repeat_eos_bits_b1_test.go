// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestRepeatEosBitsB1(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/enum_negative.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r RepeatEosBitsB1
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 16, len(r.Bits))
	assert.EqualValues(t, true, r.Bits[0])
	assert.EqualValues(t, true, r.Bits[1])
	assert.EqualValues(t, true, r.Bits[2])
	assert.EqualValues(t, true, r.Bits[3])
	assert.EqualValues(t, true, r.Bits[4])
	assert.EqualValues(t, true, r.Bits[5])
	assert.EqualValues(t, true, r.Bits[6])
	assert.EqualValues(t, true, r.Bits[7])
	assert.EqualValues(t, false, r.Bits[8])
	assert.EqualValues(t, false, r.Bits[9])
	assert.EqualValues(t, false, r.Bits[10])
	assert.EqualValues(t, false, r.Bits[11])
	assert.EqualValues(t, false, r.Bits[12])
	assert.EqualValues(t, false, r.Bits[13])
	assert.EqualValues(t, false, r.Bits[14])
	assert.EqualValues(t, true, r.Bits[15])
}
