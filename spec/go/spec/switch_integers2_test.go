// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestSwitchIntegers2(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/switch_integers.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r SwitchIntegers2
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}

	assert.EqualValues(t, 1, r.Code)
	assert.EqualValues(t, 7, r.Len)
	assert.EqualValues(t, []uint8{2, 64, 64, 4, 55, 19, 0}, r.Ham)
	assert.EqualValues(t, 0, r.Padding)
	tmp1, err := r.LenModStr()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, "13", tmp1)
}
