// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestParamsPassArrayStr(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/term_strz.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r ParamsPassArrayStr
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}

	assert.EqualValues(t, 3, len(r.PassStrArray.Strs))
	assert.EqualValues(t, "fo", r.PassStrArray.Strs[0])
	assert.EqualValues(t, "o|", r.PassStrArray.Strs[1])
	assert.EqualValues(t, "ba", r.PassStrArray.Strs[2])
	assert.EqualValues(t, 2, len(r.PassStrArrayCalc.Strs))
	assert.EqualValues(t, "aB", r.PassStrArrayCalc.Strs[0])
	assert.EqualValues(t, "Cd", r.PassStrArrayCalc.Strs[1])
}
