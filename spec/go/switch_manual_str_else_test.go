// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestSwitchManualStrElse(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/switch_opcodes2.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r SwitchManualStrElse
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 4, len(r.Opcodes))
	assert.EqualValues(t, "S", r.Opcodes[0].Code)
	assert.EqualValues(t, "foo", r.Opcodes[0].Body.(*SwitchManualStrElse_Opcode_Strval).Value)
	assert.EqualValues(t, "X", r.Opcodes[1].Code)
	assert.EqualValues(t, 66, r.Opcodes[1].Body.(*SwitchManualStrElse_Opcode_Noneval).Filler)
	assert.EqualValues(t, "Y", r.Opcodes[2].Code)
	assert.EqualValues(t, 51966, r.Opcodes[2].Body.(*SwitchManualStrElse_Opcode_Noneval).Filler)
	assert.EqualValues(t, "I", r.Opcodes[3].Code)
	assert.EqualValues(t, 7, r.Opcodes[3].Body.(*SwitchManualStrElse_Opcode_Intval).Value)
}
