// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestSwitchManualIntSizeEos(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/switch_tlv.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r SwitchManualIntSizeEos
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 4, len(r.Chunks))
	assert.EqualValues(t, 17, r.Chunks[0].Code)
	assert.EqualValues(t, "Stuff", r.Chunks[0].Body.Body.(*SwitchManualIntSizeEos_ChunkBody_ChunkMeta).Title)
	assert.EqualValues(t, "Me", r.Chunks[0].Body.Body.(*SwitchManualIntSizeEos_ChunkBody_ChunkMeta).Author)
	assert.EqualValues(t, 34, r.Chunks[1].Code)
	assert.EqualValues(t, []string{"AAAA", "BBBB", "CCCC"}, r.Chunks[1].Body.Body.(*SwitchManualIntSizeEos_ChunkBody_ChunkDir).Entries)
	assert.EqualValues(t, 51, r.Chunks[2].Code)
	assert.EqualValues(t, []uint8{16, 32, 48, 64, 80, 96, 112, 128}, r.Chunks[2].Body.Body.([]byte))
	assert.EqualValues(t, 255, r.Chunks[3].Code)
	assert.EqualValues(t, []uint8{}, r.Chunks[3].Body.Body.([]byte))
}
