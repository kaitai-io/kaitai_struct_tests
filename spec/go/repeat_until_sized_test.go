// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestRepeatUntilSized(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/repeat_until_process.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r RepeatUntilSized
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}

	assert.EqualValues(t, 3, len(r.Records))
	assert.EqualValues(t, 232, r.Records[0].Marker)
	assert.EqualValues(t, uint32(2863311546), r.Records[0].Body)
	assert.EqualValues(t, 250, r.Records[1].Marker)
	assert.EqualValues(t, uint32(2863315102), r.Records[1].Body)
	assert.EqualValues(t, 170, r.Records[2].Marker)
	assert.EqualValues(t, 1431655765, r.Records[2].Body)
}
