// Autogenerated from KST: please remove this line if doing any edits by hand!

package spec

import (
	"runtime/debug"
	"os"
	"testing"
	"github.com/kaitai-io/kaitai_struct_go_runtime/kaitai"
	. "test_formats"
	"github.com/stretchr/testify/assert"
)

func TestExprToITrailing(t *testing.T) {
	defer func() {
		if r := recover(); r != nil {
			debug.PrintStack()
			t.Fatal("unexpected panic:", r)
		}
	}()
	f, err := os.Open("../../src/term_strz.bin")
	if err != nil {
		t.Fatal(err)
	}
	s := kaitai.NewStream(f)
	var r ExprToITrailing
	err = r.Read(s, &r, &r)
	if err != nil {
		t.Fatal(err)
	}

	{
		tmp1, err := r.ToIR10()
		assert.Error(t, err)
		var wantErr strconv.NumError
		assert.ErrorAs(t, err, &wantErr)
		assert.EqualValues(t, 0, tmp1)
	}
	tmp2, err := r.ToIR16()
	if err != nil {
		t.Fatal(err)
	}
	assert.EqualValues(t, 152517308, tmp2)
	{
		tmp3, err := r.ToIGarbage()
		assert.Error(t, err)
		var wantErr strconv.NumError
		assert.ErrorAs(t, err, &wantErr)
		assert.EqualValues(t, 0, tmp3)
	}
}
