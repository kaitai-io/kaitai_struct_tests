// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.ParamsPassArrayStr;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestParamsPassArrayStr extends CommonSpec {
    @Test
    public void testParamsPassArrayStr() throws Exception {
        ParamsPassArrayStr r = ParamsPassArrayStr.fromFile(SRC_DIR + "term_strz.bin");

        assertIntEquals(r.passStrArray().strs().size(), 3);
        assertEquals(r.passStrArray().strs().get(((int) 0)), "fo");
        assertEquals(r.passStrArray().strs().get(((int) 1)), "o|");
        assertEquals(r.passStrArray().strs().get(((int) 2)), "ba");
        assertIntEquals(r.passStrArrayCalc().strs().size(), 2);
        assertEquals(r.passStrArrayCalc().strs().get(((int) 0)), "aB");
        assertEquals(r.passStrArrayCalc().strs().get(((int) 1)), "Cd");
    }
}
