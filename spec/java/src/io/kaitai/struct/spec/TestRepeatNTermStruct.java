// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.RepeatNTermStruct;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestRepeatNTermStruct extends CommonSpec {
    @Test
    public void testRepeatNTermStruct() throws Exception {
        RepeatNTermStruct r = RepeatNTermStruct.fromFile(SRC_DIR + "repeat_until_process.bin");

        assertIntEquals(r.records1().size(), 2);
        assertEquals(r.records1().get(((int) 0)).value(), new byte[] { -24, -70 });
        assertEquals(r.records1().get(((int) 1)).value(), new byte[] {  });
        assertIntEquals(r.records2().size(), 2);
        assertEquals(r.records2().get(((int) 0)).value(), new byte[] { -86 });
        assertEquals(r.records2().get(((int) 1)).value(), new byte[] { -6, -98, -72, -86 });
        assertIntEquals(r.records3().size(), 2);
        assertEquals(r.records3().get(((int) 0)).value(), new byte[] { -86, -86 });
        assertEquals(r.records3().get(((int) 1)).value(), new byte[] {  });
    }
}
