// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.ParamsPassArrayUsertype;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestParamsPassArrayUsertype extends CommonSpec {
    @Test
    public void testParamsPassArrayUsertype() throws Exception {
        ParamsPassArrayUsertype r = ParamsPassArrayUsertype.fromFile(SRC_DIR + "position_to_end.bin");

        assertIntEquals(r.passBlocks().bar().size(), 2);
        assertIntEquals(r.passBlocks().bar().get(((int) 0)).foo(), 1);
        assertIntEquals(r.passBlocks().bar().get(((int) 1)).foo(), 2);
        assertEquals(r.passBlocks().one(), new byte[] { 3 });
        assertEquals(r.passBlocks().two(), new byte[] { 4, 5 });
    }
}
