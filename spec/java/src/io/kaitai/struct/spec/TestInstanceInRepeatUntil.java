// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.InstanceInRepeatUntil;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestInstanceInRepeatUntil extends CommonSpec {
    @Test
    public void testInstanceInRepeatUntil() throws Exception {
        InstanceInRepeatUntil r = InstanceInRepeatUntil.fromFile(SRC_DIR + "repeat_until_s4.bin");

        assertIntEquals(r.entries().size(), 5);
        assertIntEquals(r.entries().get(((int) 0)), 66);
        assertIntEquals(r.entries().get(((int) 1)), 0);
        assertIntEquals(r.entries().get(((int) 2)), 4919);
        assertIntEquals(r.entries().get(((int) 3)), 0);
        assertIntEquals(r.entries().get(((int) 4)), -1);
    }
}
