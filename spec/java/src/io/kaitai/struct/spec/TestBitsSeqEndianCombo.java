// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.BitsSeqEndianCombo;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestBitsSeqEndianCombo extends CommonSpec {
    @Test
    public void testBitsSeqEndianCombo() throws Exception {
        BitsSeqEndianCombo r = BitsSeqEndianCombo.fromFile(SRC_DIR + "process_xor_4.bin");

        assertIntEquals(r.be1(), 59);
        assertIntEquals(r.be2(), 187);
        assertIntEquals(r.le3(), 163);
        assertIntEquals(r.be4(), 20);
        assertIntEquals(r.le5(), 10);
        assertIntEquals(r.le6(), 36);
        assertIntEquals(r.le7(), 26);
        assertIntEquals(r.be8(), true);
    }
}
