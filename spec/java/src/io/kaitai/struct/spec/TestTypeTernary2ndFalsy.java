// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.TypeTernary2ndFalsy;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestTypeTernary2ndFalsy extends CommonSpec {
    @Test
    public void testTypeTernary2ndFalsy() throws Exception {
        TypeTernary2ndFalsy r = TypeTernary2ndFalsy.fromFile(SRC_DIR + "switch_integers.bin");

        assertIntEquals(r.vFalse(), false);
        assertIntEquals(r.vIntZero(), 0);
        assertIntEquals(r.vIntNegZero(), 0);
        assertEquals(r.vFloatZero(), 0.0, 1e-6);
        assertEquals(r.vFloatNegZero(), -0.0, 1e-6);
        assertEquals(r.vStrWZero(), "0");
        assertIntEquals((r.vStrWZero()).length(), 1);
        assertIntEquals(r.ut().m(), 7);
        assertNull(r.vNullUt());
        assertEquals(r.vStrEmpty(), "");
        assertIntEquals((r.vStrEmpty()).length(), 0);
        assertIntEquals(r.intArray().size(), 2);
        assertIntEquals(r.vIntArrayEmpty().size(), 0);
    }
}
