// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.Expr3;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestExpr3 extends CommonSpec {
    @Test
    public void testExpr3() throws Exception {
        Expr3 r = Expr3.fromFile(SRC_DIR + "fixed_struct.bin");

        assertIntEquals(r.one(), 80);
        assertEquals(r.two(), "ACK");
        assertEquals(r.three(), "@ACK");
        assertEquals(r.four(), "_ACK_");
        assertIntEquals(r.isStrEq(), true);
        assertIntEquals(r.isStrNe(), false);
        assertIntEquals(r.isStrLt(), true);
        assertIntEquals(r.isStrGt(), false);
        assertIntEquals(r.isStrLe(), true);
        assertIntEquals(r.isStrGe(), false);
        assertIntEquals(r.isStrLt2(), true);
        assertIntEquals(r.testNot(), true);
    }
}
