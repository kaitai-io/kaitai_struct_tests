// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.ParamsPassStruct;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestParamsPassStruct extends CommonSpec {
    @Test
    public void testParamsPassStruct() throws Exception {
        ParamsPassStruct r = ParamsPassStruct.fromFile(SRC_DIR + "enum_negative.bin");

        assertIntEquals(r.first().foo(), 255);
        assertIntEquals(r.one().bar().qux(), 1);
        assertIntEquals(((ParamsPassStruct.Block) (r.one().foo())).foo(), 255);
        assertIntEquals(((ParamsPassStruct.Block) (r.one().bar().foo())).foo(), 255);
    }
}
