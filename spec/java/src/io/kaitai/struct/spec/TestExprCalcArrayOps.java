// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.ExprCalcArrayOps;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestExprCalcArrayOps extends CommonSpec {
    @Test
    public void testExprCalcArrayOps() throws Exception {
        ExprCalcArrayOps r = ExprCalcArrayOps.fromFile(SRC_DIR + "fixed_struct.bin");

        assertIntEquals(r.intArraySize(), 7);
        assertIntEquals(r.intArrayFirst(), 10);
        assertIntEquals(r.intArrayMid(), 25);
        assertIntEquals(r.intArrayLast(), 1000);
        assertIntEquals(r.intArrayMin(), 10);
        assertIntEquals(r.intArrayMax(), 1000);
        assertIntEquals(r.doubleArraySize(), 5);
        assertEquals(r.doubleArrayFirst(), 10.0, 1e-6);
        assertEquals(r.doubleArrayMid(), 25.0, 1e-6);
        assertEquals(r.doubleArrayLast(), 3.14159, 1e-6);
        assertEquals(r.doubleArrayMin(), 3.14159, 1e-6);
        assertEquals(r.doubleArrayMax(), 100.0, 1e-6);
        assertIntEquals(r.strArraySize(), 4);
        assertEquals(r.strArrayFirst(), "un");
        assertEquals(r.strArrayMid(), "deux");
        assertEquals(r.strArrayLast(), "quatre");
        assertEquals(r.strArrayMin(), "deux");
        assertEquals(r.strArrayMax(), "un");
    }
}
