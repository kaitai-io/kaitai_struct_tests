// Autogenerated from KST: please remove this line if doing any edits by hand!

package io.kaitai.struct.spec;

import io.kaitai.struct.testformats.CombineStr;
import org.testng.annotations.Test;
import static org.testng.Assert.*;
public class TestCombineStr extends CommonSpec {
    @Test
    public void testCombineStr() throws Exception {
        CombineStr r = CombineStr.fromFile(SRC_DIR + "term_strz.bin");

        assertEquals(r.strTerm(), "foo");
        assertEquals(r.strLimit(), "bar|");
        assertEquals(r.strEos(), "baz@");
        assertEquals(r.strCalc(), "bar");
        assertEquals(r.strCalcBytes(), "baz");
        assertEquals(r.termOrLimit(), "foo");
        assertEquals(r.termOrEos(), "baz@");
        assertEquals(r.termOrCalc(), "foo");
        assertEquals(r.termOrCalcBytes(), "baz");
        assertEquals(r.limitOrEos(), "bar|");
        assertEquals(r.limitOrCalc(), "bar");
        assertEquals(r.limitOrCalcBytes(), "bar|");
        assertEquals(r.eosOrCalc(), "bar");
        assertEquals(r.eosOrCalcBytes(), "baz@");
        assertEquals(r.calcOrCalcBytes(), "baz");
    }
}
