// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::index_sizes::IndexSizes;
use std::fs;

#[test]
fn test_index_sizes() {
    let data = fs::read("src/index_sizes.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let r = IndexSizes::default().read(&reader, None, KStructUnit::parent_stack()).unwrap();
    // assert_eq!(r.qty, 3);
    // assert_eq!(r.sizes[0], 1);
    // assert_eq!(r.sizes[1], 8);
    // assert_eq!(r.sizes[2], 4);
    // assert_eq!(r.bufs[0], "A");
    // assert_eq!(r.bufs[1], "BBBBBBBB");
    // assert_eq!(r.bufs[2], "CCCC");
}
