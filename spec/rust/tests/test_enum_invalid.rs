// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::enum_invalid::*;
use std::fs;

#[test]
fn test_enum_invalid() {
    let data = fs::read("../../src/term_strz.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = EnumInvalid::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    // assert_eq!(Attribute(Name(identifier(q1w2e3)),identifier(pet_1)), EnumByLabel(identifier(animal),identifier(dog),typeId(false,List(),false)))
    // nullAsset(r.pet_2);
}
