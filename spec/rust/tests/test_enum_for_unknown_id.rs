// Autogenerated from KST: please remove this line if doing any edits by hand!

use std::{fs, rc::Rc};

extern crate kaitai;
use self::kaitai::*;
mod formats;
use formats::enum_for_unknown_id::*;


#[test]
fn test_enum_for_unknown_id() {
    let bytes = fs::read("../../src/fixed_struct.bin").unwrap();
    let reader = BytesReader::new(&bytes);
    let res = EnumForUnknownId::read_into(&reader, None, None);
    let r : Rc<EnumForUnknownId>;

    if let Err(err) = res {
        panic!("{:?}", err);
    } else {
        r = res.unwrap();
    }

    let i : i64 = r.one().into();
    assert_eq!(i, 80);
}
