// Autogenerated from KST: please remove this line if doing any edits by hand!

use std::fs;
extern crate kaitai;
use self::kaitai::*;
use rust::formats::debug_array_user::*;

#[test]
fn test_debug_array_user() -> KResult<()> {
    let bytes = fs::read("../../src/fixed_struct.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let r: OptRc<DebugArrayUser> = DebugArrayUser::read_into(&_io, None, None)?;
    assert_eq!(*r.one_cat().meow(), 80);
    assert_eq!(r.array_of_cats().len(), 3);
    assert_eq!(*r.array_of_cats()[0 as usize].meow(), 65);
    assert_eq!(*r.array_of_cats()[1 as usize].meow(), 67);
    assert_eq!(*r.array_of_cats()[2 as usize].meow(), 75);
    Ok(())
}
