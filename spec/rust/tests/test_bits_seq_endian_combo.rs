// Autogenerated from KST: please remove this line if doing any edits by hand!

#![allow(unused_variables)]
#![allow(unused_assignments)]
#![allow(overflowing_literals)]
use std::fs;
extern crate kaitai;
use self::kaitai::*;
#[path = "../formats/mod.rs"] mod formats;
use formats::bits_seq_endian_combo::*;

#[test]
fn test_bits_seq_endian_combo() {
    let bytes = fs::read("../../src/process_xor_4.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let res: KResult<OptRc<BitsSeqEndianCombo>> = BitsSeqEndianCombo::read_into(&_io, None, None);
    let r : OptRc<BitsSeqEndianCombo>;

    if let Err(err) = res {
        panic!("{:?}", err);
    } else {
        r = res.unwrap();
    }

    assert_eq!(*r.be1(), 59);
    assert_eq!(*r.be2(), 187);
    assert_eq!(*r.le3(), 163);
    assert_eq!(*r.be4(), 20);
    assert_eq!(*r.le5(), 10);
    assert_eq!(*r.le6(), 36);
    assert_eq!(*r.le7(), 26);
    assert_eq!(*r.be8(), true);
}
