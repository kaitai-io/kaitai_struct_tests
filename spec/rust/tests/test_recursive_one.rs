// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::recursive_one::RecursiveOne;
use std::fs;

#[test]
fn test_recursive_one() {
    let data = fs::read("../../src/fixed_struct.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let r = RecursiveOne::default().read(&reader, None, KStructUnit::parent_stack()).unwrap();
    // assert_eq!(r.one, 80);
    // assert_eq!(r.next.one, 65);
    // assert_eq!(r.next.next.one, 67);
    // assert_eq!(r.next.next.next.finisher, 11595);
}
