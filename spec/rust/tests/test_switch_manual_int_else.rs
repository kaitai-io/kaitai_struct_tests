// Autogenerated from KST: please remove this line if doing any edits by hand!

#![allow(unused_variables)]
#![allow(unused_assignments)]
#![allow(overflowing_literals)]
use std::fs;
extern crate kaitai;
use self::kaitai::*;
use rust::formats::switch_manual_int_else::*;

#[test]
fn test_switch_manual_int_else() {
    let bytes = fs::read("../../src/switch_opcodes2.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let res: KResult<OptRc<SwitchManualIntElse>> = SwitchManualIntElse::read_into(&_io, None, None);
    let r : OptRc<SwitchManualIntElse>;

    if let Err(err) = res {
        panic!("{:?}", err);
    } else {
        r = res.unwrap();
    }

    assert_eq!(r.opcodes().len(), 4);
    assert_eq!(*r.opcodes()[0 as usize].code(), 83);
    assert_eq!(*Into::<OptRc<SwitchManualIntElse_Opcode_Strval>>::into(&*r.opcodes()[0 as usize].body().as_ref().unwrap()).value(), "foo");
    assert_eq!(*r.opcodes()[1 as usize].code(), 88);
    assert_eq!(*Into::<OptRc<SwitchManualIntElse_Opcode_Noneval>>::into(&*r.opcodes()[1 as usize].body().as_ref().unwrap()).filler(), 66);
    assert_eq!(*r.opcodes()[2 as usize].code(), 89);
    assert_eq!(*Into::<OptRc<SwitchManualIntElse_Opcode_Noneval>>::into(&*r.opcodes()[2 as usize].body().as_ref().unwrap()).filler(), 51966);
    assert_eq!(*r.opcodes()[3 as usize].code(), 73);
    assert_eq!(*Into::<OptRc<SwitchManualIntElse_Opcode_Intval>>::into(&*r.opcodes()[3 as usize].body().as_ref().unwrap()).value(), 7);
}
