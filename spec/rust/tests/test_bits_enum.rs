// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::bits_enum::BitsEnum;
use std::fs;

#[test]
fn test_bits_enum() {
    let data = fs::read("../../src/fixed_struct.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let r = BitsEnum::default().read(&reader, None, KStructUnit::parent_stack()).unwrap();
    // assert_eq!(r.one, animal::PLATYPUS);
    // assert_eq!(r.two, animal::HORSE);
    // assert_eq!(r.three, animal::CAT);
}
