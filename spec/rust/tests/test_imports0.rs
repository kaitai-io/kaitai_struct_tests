// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::imports0::*;
use std::fs;

#[test]
fn test_imports0() {
    let data = fs::read("../../src/fixed_struct.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = Imports0::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    assert_eq!(r.two, 80);
    // assert_eq!(Attribute(Attribute(Name(identifier(q1w2e3)),identifier(hw)),identifier(one)), IntNum(65))
    // assert_eq!(Attribute(Name(identifier(q1w2e3)),identifier(hw_one)), IntNum(65))
}
