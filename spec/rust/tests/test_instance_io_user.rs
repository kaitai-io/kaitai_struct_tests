// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::instance_io_user::*;
use std::fs;

#[test]
fn test_instance_io_user() {
    let data = fs::read("../../src/instance_io.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = InstanceIoUser::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    assert_eq!(r.qty_entries, 3);
    // assert_eq!(Attribute(Subscript(Attribute(Name(identifier(q1w2e3)),identifier(entries)),IntNum(0)),identifier(name)), Str(the))
    // assert_eq!(Attribute(Subscript(Attribute(Name(identifier(q1w2e3)),identifier(entries)),IntNum(1)),identifier(name)), Str(rainy))
    // assert_eq!(Attribute(Subscript(Attribute(Name(identifier(q1w2e3)),identifier(entries)),IntNum(2)),identifier(name)), Str(day it is))
}
