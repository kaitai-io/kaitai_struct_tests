// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::position_abs::*;
use std::fs;

#[test]
fn test_position_abs() {
    let data = fs::read("../../src/position_abs.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = PositionAbs::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    assert_eq!(r.index_offset, 32);
    // assert_eq!(Attribute(Attribute(Name(identifier(q1w2e3)),identifier(index)),identifier(entry)), Str(foo))
}
