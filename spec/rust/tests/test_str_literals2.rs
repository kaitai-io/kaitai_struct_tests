// Autogenerated from KST: please remove this line if doing any edits by hand!

#![allow(unused_variables)]
#![allow(unused_assignments)]
#![allow(overflowing_literals)]
use std::fs;
extern crate kaitai;
use self::kaitai::*;
#[path = "../formats/mod.rs"] mod formats;
use formats::str_literals2::*;

#[test]
fn test_str_literals2() {
    let bytes = fs::read("../../src/fixed_struct.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let res: KResult<OptRc<StrLiterals2>> = StrLiterals2::read_into(&_io, None, None);
    let r : OptRc<StrLiterals2>;

    if let Err(err) = res {
        panic!("{:?}", err);
    } else {
        r = res.unwrap();
    }

    assert_eq!(*r.dollar1().expect("error reading"), "$foo");
    assert_eq!(*r.dollar2().expect("error reading"), "${foo}");
    assert_eq!(*r.hash().expect("error reading"), "#{foo}");
    assert_eq!(*r.at_sign().expect("error reading"), "@foo");
}
