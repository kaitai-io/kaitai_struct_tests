// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::enum_negative::*;
use std::fs;

#[test]
fn test_enum_negative() {
    let data = fs::read("../../src/enum_negative.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = EnumNegative::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    // assert_eq!(Attribute(Name(identifier(q1w2e3)),identifier(f1)), EnumByLabel(identifier(constants),identifier(negative_one),typeId(false,ArrayBuffer(enum_negative),false)))
    // assert_eq!(Attribute(Name(identifier(q1w2e3)),identifier(f2)), EnumByLabel(identifier(constants),identifier(positive_one),typeId(false,ArrayBuffer(enum_negative),false)))
}
