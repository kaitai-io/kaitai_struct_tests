// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::process_coerce_usertype1::ProcessCoerceUsertype1;
use std::fs;

#[test]
fn test_process_coerce_usertype1() {
    let data = fs::read("../../src/process_coerce_bytes.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let r = ProcessCoerceUsertype1::default().read(&reader, None, KStructUnit::parent_stack()).unwrap();
    // assert_eq!(r.records[0].flag, 0);
    // assert_eq!(r.records[0].buf.value, 1094795585);
    // assert_eq!(r.records[1].flag, 1);
    // assert_eq!(r.records[1].buf.value, 1111638594);
}
