// Autogenerated from KST: please remove this line if doing any edits by hand!

use std::fs;
extern crate kaitai;
use self::kaitai::*;
use rust::formats::repeat_eos_bits_b1::*;

#[test]
fn test_repeat_eos_bits_b1() -> KResult<()> {
    let bytes = fs::read("../../src/enum_negative.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let r: OptRc<RepeatEosBitsB1> = RepeatEosBitsB1::read_into(&_io, None, None)?;
    assert_eq!(r.bits().len(), 16);
    assert_eq!(r.bits()[0 as usize], true);
    assert_eq!(r.bits()[1 as usize], true);
    assert_eq!(r.bits()[2 as usize], true);
    assert_eq!(r.bits()[3 as usize], true);
    assert_eq!(r.bits()[4 as usize], true);
    assert_eq!(r.bits()[5 as usize], true);
    assert_eq!(r.bits()[6 as usize], true);
    assert_eq!(r.bits()[7 as usize], true);
    assert_eq!(r.bits()[8 as usize], false);
    assert_eq!(r.bits()[9 as usize], false);
    assert_eq!(r.bits()[10 as usize], false);
    assert_eq!(r.bits()[11 as usize], false);
    assert_eq!(r.bits()[12 as usize], false);
    assert_eq!(r.bits()[13 as usize], false);
    assert_eq!(r.bits()[14 as usize], false);
    assert_eq!(r.bits()[15 as usize], true);
    Ok(())
}
