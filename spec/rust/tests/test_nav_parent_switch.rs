// Autogenerated from KST: please remove this line if doing any edits by hand!

#![allow(unused_variables)]
#![allow(unused_assignments)]
#![allow(overflowing_literals)]
use std::fs;
extern crate kaitai;
use self::kaitai::*;
#[path = "../formats/mod.rs"] mod formats;
use formats::nav_parent_switch::*;

#[test]
fn test_nav_parent_switch() {
    let bytes = fs::read("../../src/nav_parent_switch.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let res: KResult<OptRc<NavParentSwitch>> = NavParentSwitch::read_into(&_io, None, None);
    let r : OptRc<NavParentSwitch>;

    if let Err(err) = res {
        panic!("{:?}", err);
    } else {
        r = res.unwrap();
    }

    assert_eq!(*r.category(), 1);
    assert_eq!(*Into::<OptRc<NavParentSwitch_Element1>>::into(&*r.content().as_ref().unwrap()).foo(), 66);
    assert_eq!(*Into::<OptRc<NavParentSwitch_Element1>>::into(&*r.content().as_ref().unwrap()).subelement().bar(), 255);
}
