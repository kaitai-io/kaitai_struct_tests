// Autogenerated from KST: please remove this line if doing any edits by hand!

use std::fs;
extern crate kaitai;
use self::kaitai::*;
use rust::formats::nav_parent_switch::*;

#[test]
fn test_nav_parent_switch() -> KResult<()> {
    let bytes = fs::read("../../src/nav_parent_switch.bin").unwrap();
    let _io = BytesReader::from(bytes);
    let r: OptRc<NavParentSwitch> = NavParentSwitch::read_into(&_io, None, None)?;

    assert_eq!(*r.category(), 1);
    assert_eq!(*Into::<OptRc<NavParentSwitch_Element1>>::into(&*r.content().as_ref().unwrap()).foo(), 66);
    assert_eq!(*Into::<OptRc<NavParentSwitch_Element1>>::into(&*r.content().as_ref().unwrap()).subelement().bar(), 255);
    Ok(())
}
