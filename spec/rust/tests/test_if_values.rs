// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::if_values::IfValues;
use std::fs;

#[test]
fn test_if_values() {
    let data = fs::read("../../src/fixed_struct.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let r = IfValues::default().read(&reader, None, KStructUnit::parent_stack()).unwrap();
    // assert_eq!(r.codes[0].opcode, 80);
    // assert_eq!(r.codes[0].half_opcode, 40);
    // assert_eq!(r.codes[1].opcode, 65);
    // nullAsset(r.codes[1].half_opcode);
    // assert_eq!(r.codes[2].opcode, 67);
    // nullAsset(r.codes[2].half_opcode);
}
