// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::repeat_until_s4::*;
use std::fs;

#[test]
fn test_repeat_until_s4() {
    let data = fs::read("../../src/repeat_until_s4.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = RepeatUntilS4::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    assert_eq!(r.entries, [66, 4919, -251658241, -1]);
    assert_eq!(r.afterall, "foobar");
}
