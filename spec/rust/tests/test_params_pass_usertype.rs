// Autogenerated from KST: please remove this line if doing any edits by hand!

use kaitai::{BytesReader, KStruct, KStructUnit};
use kaitai_test::params_pass_usertype::*;
use std::fs;

#[test]
fn test_params_pass_usertype() {
    let data = fs::read("../../src/position_in_seq.bin").expect("Unable to read data.");
    let reader = BytesReader::new(&data[..]);
    let mut r = ParamsPassUsertype::default();
    r.read(&reader, None, KStructUnit::parent_stack()).expect("Unable to parse data.");
    // assert_eq!(Attribute(Attribute(Name(identifier(q1w2e3)),identifier(first)),identifier(foo)), IntNum(1))
    // assert_eq!(Attribute(Attribute(Name(identifier(q1w2e3)),identifier(one)),identifier(buf)), List(ArrayBuffer(IntNum(2))))
}
