# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest

from reserved_python_keywords import _schema

class TestReservedPythonKeywords(unittest.TestCase):
    def test_reserved_python_keywords(self):
        r = _schema.parse_file('src/full256.bin')
        self.assertEqual(r.and, 0)
        self.assertEqual(r.as, 1)
        self.assertEqual(r.assert, 2)
        self.assertEqual(r.async, 3)
        self.assertEqual(r.await, 4)
        self.assertEqual(r.break, 5)
        self.assertEqual(r.class, 6)
        self.assertEqual(r.continue, 7)
        self.assertEqual(r.def, 8)
        self.assertEqual(r.del, 9)
        self.assertEqual(r.elif, 10)
        self.assertEqual(r.else, 11)
        self.assertEqual(r.except, 12)
        self.assertEqual(r.false, 13)
        self.assertEqual(r.finally, 14)
        self.assertEqual(r.for, 15)
        self.assertEqual(r.from, 16)
        self.assertEqual(r.global, 17)
        self.assertEqual(r.if, 18)
        self.assertEqual(r.import, 19)
        self.assertEqual(r.in, 20)
        self.assertEqual(r.is, 21)
        self.assertEqual(r.lambda, 22)
        self.assertEqual(r.none, 23)
        self.assertEqual(r.nonlocal, 24)
        self.assertEqual(r.not, 25)
        self.assertEqual(r.or, 26)
        self.assertEqual(r.pass, 27)
        self.assertEqual(r.raise, 28)
        self.assertEqual(r.return, 29)
        self.assertEqual(r.true, 30)
        self.assertEqual(r.try, 31)
        self.assertEqual(r.while, 32)
        self.assertEqual(r.with, 33)
        self.assertEqual(r.yield, 34)
        self.assertEqual(r.keyword_struct_def.while, 35)
        self.assertEqual(r.keyword_struct_def.for, 36)
        self.assertEqual(r.keyword_struct_def.with, 37)
        self.assertEqual(r.keyword_struct_false.pass, 38)
        self.assertEqual(r.keyword_enum, 'and')
        self.assertEqual(r.keyword_nested_enum, 'except')
        self.assertEqual(r.inst_keyword_seq, 1)
        self.assertEqual(r.inst_keyword_enum, 'if')
        self.assertEqual(r.inst_keyword_enum_to_i, 6)
        self.assertEqual(r.keyword_struct_def.return, 71)
