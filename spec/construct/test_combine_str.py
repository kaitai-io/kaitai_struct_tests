# Autogenerated from KST: please remove this line if doing any edits by hand!

import unittest

from combine_str import _schema

class TestCombineStr(unittest.TestCase):
    def test_combine_str(self):
        r = _schema.parse_file('src/term_strz.bin')
        self.assertEqual(r.str_term, "foo")
        self.assertEqual(r.str_limit, "bar|")
        self.assertEqual(r.str_eos, "baz@")
        self.assertEqual(r.str_calc, "bar")
        self.assertEqual(r.str_calc_bytes, "baz")
        self.assertEqual(r.term_or_limit, "foo")
        self.assertEqual(r.term_or_eos, "baz@")
        self.assertEqual(r.term_or_calc, "foo")
        self.assertEqual(r.term_or_calc_bytes, "baz")
        self.assertEqual(r.limit_or_eos, "bar|")
        self.assertEqual(r.limit_or_calc, "bar")
        self.assertEqual(r.limit_or_calc_bytes, "bar|")
        self.assertEqual(r.eos_or_calc, "bar")
        self.assertEqual(r.eos_or_calc_bytes, "baz@")
        self.assertEqual(r.calc_or_calc_bytes, "baz")
