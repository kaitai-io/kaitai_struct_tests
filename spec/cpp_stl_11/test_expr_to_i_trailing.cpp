// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "expr_to_i_trailing.h"
#include <iostream>
#include <fstream>
#include <vector>
#include "kaitai/exceptions.h"

BOOST_AUTO_TEST_CASE(test_expr_to_i_trailing) {
    std::ifstream ifs("src/term_strz.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    expr_to_i_trailing_t* r = new expr_to_i_trailing_t(&ks);

    BOOST_CHECK_THROW(
        r->to_i_r10(),
        std::invalid_argument
    );
    BOOST_CHECK_EQUAL(r->to_i_r16(), 152517308);
    BOOST_CHECK_THROW(
        r->to_i_garbage(),
        std::invalid_argument
    );

    delete r;
}
