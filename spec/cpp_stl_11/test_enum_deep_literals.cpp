// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "enum_deep_literals.h"
#include <iostream>
#include <fstream>
#include <vector>

BOOST_AUTO_TEST_CASE(test_enum_deep_literals) {
    std::ifstream ifs("src/enum_0.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    enum_deep_literals_t* r = new enum_deep_literals_t(&ks);

    BOOST_CHECK_EQUAL(r->is_pet_1_ok(), true);
    BOOST_CHECK_EQUAL(r->is_pet_2_ok(), true);

    delete r;
}
