// Autogenerated from KST: please remove this line if doing any edits by hand!

#include <boost/test/unit_test.hpp>
#include "repeat_eos_struct.h"
#include <iostream>
#include <fstream>
#include <vector>

BOOST_AUTO_TEST_CASE(test_repeat_eos_struct) {
    std::ifstream ifs("src/repeat_eos_struct.bin", std::ifstream::binary);
    kaitai::kstream ks(&ifs);
    repeat_eos_struct_t* r = new repeat_eos_struct_t(&ks);

    BOOST_CHECK_EQUAL(r->chunks()->size(), 2);
    BOOST_CHECK_EQUAL(r->chunks()->at(0)->offset(), 0);
    BOOST_CHECK_EQUAL(r->chunks()->at(0)->len(), 66);
    BOOST_CHECK_EQUAL(r->chunks()->at(1)->offset(), 66);
    BOOST_CHECK_EQUAL(r->chunks()->at(1)->len(), 2069);

    delete r;
}
