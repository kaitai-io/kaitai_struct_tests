// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_repeat_usertype_dynarg_xor = @import("../formats/process_repeat_usertype_dynarg_xor.zig");

test "ProcessRepeatUsertypeDynargXor" {
    const file = try std.fs.cwd().openFile("../../src/process_xor_4.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_repeat_usertype_dynarg_xor.ProcessRepeatUsertypeDynargXor.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2319263090, r.blocks.items[0].a);
    try std.testing.expectEqual(263540053, r.blocks.items[1].a);
    try std.testing.expectEqual(209, r.blocks_b.dummy);
    try std.testing.expectEqual(20, (try r.blocks_b.blocks0B()));
    try std.testing.expectEqual(91, (try r.blocks_b.blocks1B()));
}
