// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_coerce_usertype2 = @import("../formats/process_coerce_usertype2.zig");

test "ProcessCoerceUsertype2" {
    const file = try std.fs.cwd().openFile("../../src/process_coerce_bytes.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_coerce_usertype2.ProcessCoerceUsertype2.create(&arena, &_io, null, null);
    try std.testing.expectEqual(0, r.records.items[0].flag);
    try std.testing.expectEqual(1094795585, (try r.records.items[0].buf()).value);
    try std.testing.expectEqual(1, r.records.items[1].flag);
    try std.testing.expectEqual(1111638594, (try r.records.items[1].buf()).value);
}
