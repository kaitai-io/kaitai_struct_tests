// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nav_parent_false = @import("../formats/nav_parent_false.zig");

test "NavParentFalse" {
    const file = try std.fs.cwd().openFile("../../src/nav_parent_codes.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nav_parent_false.NavParentFalse.create(&arena, &_io, null, null);
    try std.testing.expectEqual(3, r.child_size);
    try std.testing.expectEqual(73, r.element_a.foo.code);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 49, 50, 51 }, r.element_a.foo.more);
    try std.testing.expectEqual(66, r.element_a.bar.foo.code);
    try std.testing.expectEqual(98, r.element_b.foo.code);
}
