// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_repeat_until_sized = @import("../formats/repeat_until_sized.zig");

test "RepeatUntilSized" {
    const file = try _imp_std.fs.cwd().openFile("../../src/repeat_until_process.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_repeat_until_sized.RepeatUntilSized.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(3, r.records.items.len);
    try _imp_std.testing.expectEqual(232, r.records.items[0].marker);
    try _imp_std.testing.expectEqual(2863311546, r.records.items[0].body);
    try _imp_std.testing.expectEqual(250, r.records.items[1].marker);
    try _imp_std.testing.expectEqual(2863315102, r.records.items[1].body);
    try _imp_std.testing.expectEqual(170, r.records.items[2].marker);
    try _imp_std.testing.expectEqual(1431655765, r.records.items[2].body);
}
