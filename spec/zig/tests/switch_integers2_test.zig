// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const switch_integers2 = @import("../formats/switch_integers2.zig");

test "SwitchIntegers2" {
    const file = try std.fs.cwd().openFile("../../src/switch_integers.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try switch_integers2.SwitchIntegers2.create(&arena, &_io, null, null);
    try std.testing.expectEqual(1, r.code);
    try std.testing.expectEqual(7, r.len);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 2, 64, 64, 4, 55, 19, 0 }, r.ham);
    try std.testing.expectEqual(0, r.padding);
    try std.testing.expectEqualStrings("13", (try r.lenModStr()));
}
