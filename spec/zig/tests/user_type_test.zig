// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const user_type = @import("../formats/user_type.zig");

test "UserType" {
    const file = try std.fs.cwd().openFile("../../src/repeat_until_s4.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try user_type.UserType.create(&arena, &_io, null, null);
    try std.testing.expectEqual(66, r.one.width);
    try std.testing.expectEqual(4919, r.one.height);
}
