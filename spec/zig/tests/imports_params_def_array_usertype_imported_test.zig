// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const imports_params_def_array_usertype_imported = @import("../formats/imports_params_def_array_usertype_imported.zig");

test "ImportsParamsDefArrayUsertypeImported" {
    const file = try std.fs.cwd().openFile("../../src/process_xor_4.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try imports_params_def_array_usertype_imported.ImportsParamsDefArrayUsertypeImported.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.hws.items.len);
    try std.testing.expectEqual(236, r.hws.items[0].one);
    try std.testing.expectEqual(187, r.hws.items[1].one);
    try std.testing.expectEqual(236, (try r.two.hw0One()));
    try std.testing.expectEqual(187, (try r.two.hw1One()));
}
