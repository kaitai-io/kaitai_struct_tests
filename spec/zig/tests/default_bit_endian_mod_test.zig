// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const default_bit_endian_mod = @import("../formats/default_bit_endian_mod.zig");

test "DefaultBitEndianMod" {
    const file = try std.fs.cwd().openFile("../../src/fixed_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try default_bit_endian_mod.DefaultBitEndianMod.create(&arena, &_io, null, null);
    try std.testing.expectEqual(336, r.main.one);
    try std.testing.expectEqual(8608, r.main.two);
    try std.testing.expectEqual(11595, r.main.nest.two);
    try std.testing.expectEqual(12799, r.main.nest_be.two);
}
