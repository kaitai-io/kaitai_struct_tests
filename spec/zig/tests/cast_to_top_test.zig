// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const cast_to_top = @import("../formats/cast_to_top.zig");

test "CastToTop" {
    const file = try std.fs.cwd().openFile("../../src/fixed_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try cast_to_top.CastToTop.create(&arena, &_io, null, null);
    try std.testing.expectEqual(80, r.code);
    try std.testing.expectEqual(65, (try r.header()).code);
    try std.testing.expectEqual(65, (try r.headerCasted()).code);
}
