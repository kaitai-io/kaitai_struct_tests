// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_switch_integers = @import("../formats/switch_integers.zig");

test "SwitchIntegers" {
    const file = try _imp_std.fs.cwd().openFile("../../src/switch_integers.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_switch_integers.SwitchIntegers.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(4, r.opcodes.items.len);
    try _imp_std.testing.expectEqual(1, r.opcodes.items[0].code);
    try _imp_std.testing.expectEqual(7, r.opcodes.items[0].body);
    try _imp_std.testing.expectEqual(2, r.opcodes.items[1].code);
    try _imp_std.testing.expectEqual(16448, r.opcodes.items[1].body);
    try _imp_std.testing.expectEqual(4, r.opcodes.items[2].code);
    try _imp_std.testing.expectEqual(4919, r.opcodes.items[2].body);
    try _imp_std.testing.expectEqual(8, r.opcodes.items[3].code);
    try _imp_std.testing.expectEqual(4919, r.opcodes.items[3].body);
}
