// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const enum_to_i = @import("../formats/enum_to_i.zig");

test "EnumToI" {
    const file = try std.fs.cwd().openFile("../../src/enum_0.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try enum_to_i.EnumToI.create(&arena, &_io, null, null);
    try std.testing.expectEqual(enum_to_i.EnumToI.Animal.cat, r.pet_1);
    try std.testing.expectEqual(enum_to_i.EnumToI.Animal.chicken, r.pet_2);
    try std.testing.expectEqual(7, (try r.pet1I()));
    try std.testing.expectEqualStrings("7", (try r.pet1IToS()));
    try std.testing.expectEqual(32775, (try r.pet1Mod()));
    try std.testing.expectEqual(true, (try r.oneLtTwo()));
    try std.testing.expectEqual(true, (try r.pet1EqInt()));
    try std.testing.expectEqual(false, (try r.pet2EqInt()));
}
