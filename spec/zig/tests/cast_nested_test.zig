// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const cast_nested = @import("../formats/cast_nested.zig");

test "CastNested" {
    const file = try std.fs.cwd().openFile("../../src/switch_opcodes.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try cast_nested.CastNested.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("foobar", (try r.opcodes0Str()).value);
    try std.testing.expectEqualStrings("foobar", (try r.opcodes0StrValue()));
    try std.testing.expectEqual(66, (try r.opcodes1Int()).value);
    try std.testing.expectEqual(66, (try r.opcodes1IntValue()));
}
