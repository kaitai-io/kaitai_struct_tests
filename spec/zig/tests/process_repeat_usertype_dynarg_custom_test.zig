// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_repeat_usertype_dynarg_custom = @import("../formats/process_repeat_usertype_dynarg_custom.zig");

test "ProcessRepeatUsertypeDynargCustom" {
    const file = try std.fs.cwd().openFile("../../src/process_rotate.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_repeat_usertype_dynarg_custom.ProcessRepeatUsertypeDynargCustom.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2290657028, r.blocks.items[0].a);
    try std.testing.expectEqual(2057999057, r.blocks.items[1].a);
    try std.testing.expectEqual(1, r.blocks_b.dummy);
    try std.testing.expectEqual(232, (try r.blocks_b.blocks0B()));
    try std.testing.expectEqual(58, (try r.blocks_b.blocks1B()));
}
