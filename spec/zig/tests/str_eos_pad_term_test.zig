// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const str_eos_pad_term = @import("../formats/str_eos_pad_term.zig");

test "StrEosPadTerm" {
    const file = try std.fs.cwd().openFile("../../src/str_pad_term.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try str_eos_pad_term.StrEosPadTerm.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("str1", r.str_pad.value);
    try std.testing.expectEqualStrings("str2foo", r.str_term.value);
    try std.testing.expectEqualStrings("str+++3bar+++", r.str_term_and_pad.value);
    try std.testing.expectEqualStrings("str4baz@", r.str_term_include.value);
}
