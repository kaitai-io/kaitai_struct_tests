// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_combine_str = @import("../formats/combine_str.zig");

test "CombineStr" {
    const file = try _imp_std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_combine_str.CombineStr.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqualStrings("foo", r.str_term);
    try _imp_std.testing.expectEqualStrings("bar|", r.str_limit);
    try _imp_std.testing.expectEqualStrings("baz@", r.str_eos);
    try _imp_std.testing.expectEqualStrings("bar", (try r.strCalc()));
    try _imp_std.testing.expectEqualStrings("baz", (try r.strCalcBytes()));
    try _imp_std.testing.expectEqualStrings("foo", (try r.termOrLimit()));
    try _imp_std.testing.expectEqualStrings("baz@", (try r.termOrEos()));
    try _imp_std.testing.expectEqualStrings("foo", (try r.termOrCalc()));
    try _imp_std.testing.expectEqualStrings("baz", (try r.termOrCalcBytes()));
    try _imp_std.testing.expectEqualStrings("bar|", (try r.limitOrEos()));
    try _imp_std.testing.expectEqualStrings("bar", (try r.limitOrCalc()));
    try _imp_std.testing.expectEqualStrings("bar|", (try r.limitOrCalcBytes()));
    try _imp_std.testing.expectEqualStrings("bar", (try r.eosOrCalc()));
    try _imp_std.testing.expectEqualStrings("baz@", (try r.eosOrCalcBytes()));
    try _imp_std.testing.expectEqualStrings("baz", (try r.calcOrCalcBytes()));
}
