// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nav_parent_switch_cast = @import("../formats/nav_parent_switch_cast.zig");

test "NavParentSwitchCast" {
    const file = try std.fs.cwd().openFile("../../src/switch_integers.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nav_parent_switch_cast.NavParentSwitchCast.create(&arena, &_io, null, null);
    try std.testing.expectEqual(1, r.main.buf_type);
    try std.testing.expectEqual(7, r.main.flag);
    try std.testing.expectEqual(7, (try @as(*nav_parent_switch_cast.NavParentSwitchCast.Foo.One, r.main.buf).branch.flag()));
}
