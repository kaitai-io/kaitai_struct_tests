// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_if_int_ops = @import("../formats/expr_if_int_ops.zig");

test "ExprIfIntOps" {
    const file = try std.fs.cwd().openFile("../../src/instance_io.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_if_int_ops.ExprIfIntOps.create(&arena, &_io, null, null);
    try std.testing.expectEqual(3, r.key);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 252, 252, 252, 253, 9, 3, 3, 3 }, r.bytes);
    try std.testing.expectEqual(253, (try r.bytesSubKey()));
    try std.testing.expectEqual(-3, (try r.itemsSubKey()));
}
