// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const switch_manual_enum_invalid = @import("../formats/switch_manual_enum_invalid.zig");

test "SwitchManualEnumInvalid" {
    const file = try std.fs.cwd().openFile("../../src/enum_negative.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try switch_manual_enum_invalid.SwitchManualEnumInvalid.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.opcodes.items.len);
    try std.testing.expectEqual(255, r.opcodes.items[0].code);
    try std.testing.expectEqual(null, r.opcodes.items[0].body);
    try std.testing.expectEqual(1, r.opcodes.items[1].code);
    try std.testing.expectEqual(null, r.opcodes.items[1].body);
}
