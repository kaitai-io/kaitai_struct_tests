// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const debug_switch_user = @import("../formats/debug_switch_user.zig");

test "DebugSwitchUser" {
    const file = try std.fs.cwd().openFile("../../src/nav_parent_switch.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try debug_switch_user.DebugSwitchUser.create(&arena, &_io, null, null);
    try std.testing.expectEqual(1, r.code);
    try std.testing.expectEqual(-190, @as(*debug_switch_user.DebugSwitchUser.One, r.data).val);
}
