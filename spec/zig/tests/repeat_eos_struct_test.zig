// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const repeat_eos_struct = @import("../formats/repeat_eos_struct.zig");

test "RepeatEosStruct" {
    const file = try std.fs.cwd().openFile("../../src/repeat_eos_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try repeat_eos_struct.RepeatEosStruct.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.chunks.items.len);
    try std.testing.expectEqual(0, r.chunks.items[0].offset);
    try std.testing.expectEqual(66, r.chunks.items[0].len);
    try std.testing.expectEqual(66, r.chunks.items[1].offset);
    try std.testing.expectEqual(2069, r.chunks.items[1].len);
}
