// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_term_struct = @import("../formats/process_term_struct.zig");

test "ProcessTermStruct" {
    const file = try std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_term_struct.ProcessTermStruct.create(&arena, &_io, null, null);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 70, 79, 79 }, r.s1.value);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 66, 65, 82 }, r.s2.value);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 92, 66, 65, 90, 32 }, r.s3.value);
}
