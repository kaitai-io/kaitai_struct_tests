// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_repeat_until_complex = @import("../formats/repeat_until_complex.zig");

test "RepeatUntilComplex" {
    const file = try _imp_std.fs.cwd().openFile("../../src/repeat_until_complex.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_repeat_until_complex.RepeatUntilComplex.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(3, r.first.items.len);
    try _imp_std.testing.expectEqual(4, r.first.items[0].count);
    try _imp_std.testing.expectEqualSlices(u8, &.{ 0 + 1, 2, 3, 4 }, r.first.items[0].values.items);
    try _imp_std.testing.expectEqual(2, r.first.items[1].count);
    try _imp_std.testing.expectEqualSlices(u8, &.{ 0 + 1, 2 }, r.first.items[1].values.items);
    try _imp_std.testing.expectEqual(0, r.first.items[2].count);
    try _imp_std.testing.expectEqual(4, r.second.items.len);
    try _imp_std.testing.expectEqual(6, r.second.items[0].count);
    try _imp_std.testing.expectEqualSlices(u16, &.{ 0 + 1, 2, 3, 4, 5, 6 }, r.second.items[0].values.items);
    try _imp_std.testing.expectEqual(3, r.second.items[1].count);
    try _imp_std.testing.expectEqualSlices(u16, &.{ 0 + 1, 2, 3 }, r.second.items[1].values.items);
    try _imp_std.testing.expectEqual(4, r.second.items[2].count);
    try _imp_std.testing.expectEqualSlices(u16, &.{ 0 + 1, 2, 3, 4 }, r.second.items[2].values.items);
    try _imp_std.testing.expectEqual(0, r.second.items[3].count);
    try _imp_std.testing.expectEqualSlices(u8, &.{ 0 + 102, 111, 111, 98, 97, 114, 0 }, r.third.items);
}
