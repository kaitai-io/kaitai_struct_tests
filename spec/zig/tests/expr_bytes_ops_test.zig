// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_expr_bytes_ops = @import("../formats/expr_bytes_ops.zig");

test "ExprBytesOps" {
    const file = try _imp_std.fs.cwd().openFile("../../src/nav_parent_switch.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_expr_bytes_ops.ExprBytesOps.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(3, (try r.oneSize()));
    try _imp_std.testing.expectEqual(1, (try r.oneFirst()));
    try _imp_std.testing.expectEqual(66, (try r.oneMid()));
    try _imp_std.testing.expectEqual(255, (try r.oneLast()));
    try _imp_std.testing.expectEqualStrings("255", (try _imp_std.fmt.allocPrint(allocator, "{d}", .{ (try r.oneLast()) })));
    try _imp_std.testing.expectEqual(1, (try r.oneMin()));
    try _imp_std.testing.expectEqual(255, (try r.oneMax()));
    try _imp_std.testing.expectEqualStrings("255", (try _imp_std.fmt.allocPrint(allocator, "{d}", .{ (try r.oneMax()) })));
    try _imp_std.testing.expectEqual(3, (try r.twoSize()));
    try _imp_std.testing.expectEqual(65, (try r.twoFirst()));
    try _imp_std.testing.expectEqual(255, (try r.twoMid()));
    try _imp_std.testing.expectEqualStrings("255", (try _imp_std.fmt.allocPrint(allocator, "{d}", .{ (try r.twoMid()) })));
    try _imp_std.testing.expectEqual(75, (try r.twoLast()));
    try _imp_std.testing.expectEqual(65, (try r.twoMin()));
    try _imp_std.testing.expectEqual(255, (try r.twoMax()));
    try _imp_std.testing.expectEqualStrings("255", (try _imp_std.fmt.allocPrint(allocator, "{d}", .{ (try r.twoMax()) })));
}
