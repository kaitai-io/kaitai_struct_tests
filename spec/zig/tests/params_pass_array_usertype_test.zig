// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const params_pass_array_usertype = @import("../formats/params_pass_array_usertype.zig");

test "ParamsPassArrayUsertype" {
    const file = try std.fs.cwd().openFile("../../src/position_to_end.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try params_pass_array_usertype.ParamsPassArrayUsertype.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.pass_blocks.bar.items.len);
    try std.testing.expectEqual(1, r.pass_blocks.bar.items[0].foo);
    try std.testing.expectEqual(2, r.pass_blocks.bar.items[1].foo);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 3 }, r.pass_blocks.one);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 4, 5 }, r.pass_blocks.two);
}
