// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_io_pos = @import("../formats/expr_io_pos.zig");

test "ExprIoPos" {
    const file = try std.fs.cwd().openFile("../../src/expr_io_pos.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_io_pos.ExprIoPos.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("CURIOSITY", r.substream1.my_str);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 17, 34, 51, 68 }, r.substream1.body);
    try std.testing.expectEqual(66, r.substream1.number);
    try std.testing.expectEqualStrings("KILLED", r.substream2.my_str);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 97, 32, 99, 97, 116 }, r.substream2.body);
    try std.testing.expectEqual(103, r.substream2.number);
}
