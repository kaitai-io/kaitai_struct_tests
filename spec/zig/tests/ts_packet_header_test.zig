// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_ts_packet_header = @import("../formats/ts_packet_header.zig");

test "TsPacketHeader" {
    const file = try _imp_std.fs.cwd().openFile("../../src/ts_packet.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_ts_packet_header.TsPacketHeader.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(71, r.sync_byte);
    try _imp_std.testing.expectEqual(false, r.transport_error_indicator);
    try _imp_std.testing.expectEqual(false, r.payload_unit_start_indicator);
    try _imp_std.testing.expectEqual(true, r.transport_priority);
    try _imp_std.testing.expectEqual(33, r.pid);
    try _imp_std.testing.expectEqual(0, r.transport_scrambling_control);
    try _imp_std.testing.expectEqual(_imp_ts_packet_header.TsPacketHeader.AdaptationFieldControlEnum.payload_only, r.adaptation_field_control);
}
