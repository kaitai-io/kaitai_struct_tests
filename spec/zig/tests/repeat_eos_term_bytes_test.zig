// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const repeat_eos_term_bytes = @import("../formats/repeat_eos_term_bytes.zig");

test "RepeatEosTermBytes" {
    const file = try std.fs.cwd().openFile("../../src/process_rotate.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try repeat_eos_term_bytes.RepeatEosTermBytes.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.records.items.len);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 9, 172, 141, 141, 237, 186, 123, 147, 99, 35, 1, 42, 52, 178 }, r.records.items[0]);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 57, 178 }, r.records.items[1]);
}
