// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const instance_io_user = @import("../formats/instance_io_user.zig");

test "InstanceIoUser" {
    const file = try std.fs.cwd().openFile("../../src/instance_io.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try instance_io_user.InstanceIoUser.create(&arena, &_io, null, null);
    try std.testing.expectEqual(3, r.qty_entries);
    try std.testing.expectEqualStrings("the", (try r.entries.items[0].name()));
    try std.testing.expectEqualStrings("rainy", (try r.entries.items[1].name()));
    try std.testing.expectEqualStrings("day it is", (try r.entries.items[2].name()));
}
