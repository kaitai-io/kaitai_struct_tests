// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_instance_io_user = @import("../formats/instance_io_user.zig");

test "InstanceIoUser" {
    const file = try _imp_std.fs.cwd().openFile("../../src/instance_io.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_instance_io_user.InstanceIoUser.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(3, r.qty_entries);
    try _imp_std.testing.expectEqualStrings("the", (try r.entries.items[0].name()));
    try _imp_std.testing.expectEqualStrings("rainy", (try r.entries.items[1].name()));
    try _imp_std.testing.expectEqualStrings("day it is", (try r.entries.items[2].name()));
}
