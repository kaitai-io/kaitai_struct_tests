// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_enum = @import("../formats/expr_enum.zig");

test "ExprEnum" {
    const file = try std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_enum.ExprEnum.create(&arena, &_io, null, null);
    try std.testing.expectEqual(expr_enum.ExprEnum.Animal.dog, (try r.constDog()));
    try std.testing.expectEqual(expr_enum.ExprEnum.Animal.boom, (try r.derivedBoom()));
    try std.testing.expectEqual(expr_enum.ExprEnum.Animal.dog, (try r.derivedDog()));
}
