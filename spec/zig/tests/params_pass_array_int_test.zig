// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const params_pass_array_int = @import("../formats/params_pass_array_int.zig");

test "ParamsPassArrayInt" {
    const file = try std.fs.cwd().openFile("../../src/position_to_end.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try params_pass_array_int.ParamsPassArrayInt.create(&arena, &_io, null, null);
    try std.testing.expectEqual(3, r.pass_ints.nums.items.len);
    try std.testing.expectEqual(513, r.pass_ints.nums.items[0]);
    try std.testing.expectEqual(1027, r.pass_ints.nums.items[1]);
    try std.testing.expectEqual(1541, r.pass_ints.nums.items[2]);
    try std.testing.expectEqual(2, r.pass_ints_calc.nums.items.len);
    try std.testing.expectEqual(27643, r.pass_ints_calc.nums.items[0]);
    try std.testing.expectEqual(7, r.pass_ints_calc.nums.items[1]);
}
