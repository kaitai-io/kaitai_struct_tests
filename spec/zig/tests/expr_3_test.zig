// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_expr_3 = @import("../formats/expr_3.zig");

test "Expr3" {
    const file = try _imp_std.fs.cwd().openFile("../../src/fixed_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_expr_3.Expr3.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(80, r.one);
    try _imp_std.testing.expectEqualStrings("ACK", r.two);
    try _imp_std.testing.expectEqualStrings("@ACK", (try r.three()));
    try _imp_std.testing.expectEqualStrings("_ACK_", (try r.four()));
    try _imp_std.testing.expectEqual(true, (try r.isStrEq()));
    try _imp_std.testing.expectEqual(false, (try r.isStrNe()));
    try _imp_std.testing.expectEqual(true, (try r.isStrLt()));
    try _imp_std.testing.expectEqual(false, (try r.isStrGt()));
    try _imp_std.testing.expectEqual(true, (try r.isStrLe()));
    try _imp_std.testing.expectEqual(false, (try r.isStrGe()));
    try _imp_std.testing.expectEqual(true, (try r.isStrLt2()));
    try _imp_std.testing.expectEqual(true, (try r.testNot()));
}
