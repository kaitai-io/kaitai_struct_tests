// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_expr_bytes_cmp = @import("../formats/expr_bytes_cmp.zig");

test "ExprBytesCmp" {
    const file = try _imp_std.fs.cwd().openFile("../../src/fixed_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_expr_bytes_cmp.ExprBytesCmp.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 80 }, r.one);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 65, 67, 75 }, r.two);
    try _imp_std.testing.expectEqual(true, (try r.isEq()));
    try _imp_std.testing.expectEqual(false, (try r.isNe()));
    try _imp_std.testing.expectEqual(true, (try r.isLt()));
    try _imp_std.testing.expectEqual(false, (try r.isGt()));
    try _imp_std.testing.expectEqual(true, (try r.isLe()));
    try _imp_std.testing.expectEqual(false, (try r.isGe()));
    try _imp_std.testing.expectEqual(false, (try r.isLt2()));
    try _imp_std.testing.expectEqual(true, (try r.isGt2()));
}
