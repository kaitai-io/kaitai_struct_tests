// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_coerce_switch = @import("../formats/process_coerce_switch.zig");

test "ProcessCoerceSwitch" {
    const file = try std.fs.cwd().openFile("../../src/process_coerce_switch.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_coerce_switch.ProcessCoerceSwitch.create(&arena, &_io, null, null);
    try std.testing.expectEqual(0, r.buf_type);
    try std.testing.expectEqual(0, r.flag);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 65, 65, 65, 65 }, @as(*process_coerce_switch.ProcessCoerceSwitch.Foo, (try r.buf())).bar);
}
