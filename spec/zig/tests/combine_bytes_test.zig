// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_combine_bytes = @import("../formats/combine_bytes.zig");

test "CombineBytes" {
    const file = try _imp_std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_combine_bytes.CombineBytes.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 102, 111, 111 }, r.bytes_term);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 98, 97, 114, 124 }, r.bytes_limit);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 98, 97, 122, 64 }, r.bytes_eos);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 82, 110, 68 }, (try r.bytesCalc()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 102, 111, 111 }, (try r.termOrLimit()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 98, 97, 122, 64 }, (try r.termOrEos()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 102, 111, 111 }, (try r.termOrCalc()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 98, 97, 114, 124 }, (try r.limitOrEos()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 82, 110, 68 }, (try r.limitOrCalc()));
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 98, 97, 122, 64 }, (try r.eosOrCalc()));
}
