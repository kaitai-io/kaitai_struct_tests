// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_process_coerce_bytes = @import("../formats/process_coerce_bytes.zig");

test "ProcessCoerceBytes" {
    const file = try _imp_std.fs.cwd().openFile("../../src/process_coerce_bytes.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_process_coerce_bytes.ProcessCoerceBytes.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(0, r.records.items[0].flag);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 65, 65, 65, 65 }, (try r.records.items[0].buf()));
    try _imp_std.testing.expectEqual(1, r.records.items[1].flag);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 66, 66, 66, 66 }, (try r.records.items[1].buf()));
}
