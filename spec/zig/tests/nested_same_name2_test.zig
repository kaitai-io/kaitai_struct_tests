// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nested_same_name2 = @import("../formats/nested_same_name2.zig");

test "NestedSameName2" {
    const file = try std.fs.cwd().openFile("../../src/nested_same_name2.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nested_same_name2.NestedSameName2.create(&arena, &_io, null, null);
    try std.testing.expectEqual(66, r.version);
    try std.testing.expectEqual(2, r.main_data.main_size);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 17, 17, 17, 17 }, r.main_data.foo.data1);
    try std.testing.expectEqual(3, r.dummy.dummy_size);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 34, 34, 34, 34, 34, 34 }, r.dummy.foo.data2);
}
