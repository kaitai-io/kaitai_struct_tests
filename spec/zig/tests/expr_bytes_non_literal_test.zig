// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_bytes_non_literal = @import("../formats/expr_bytes_non_literal.zig");

test "ExprBytesNonLiteral" {
    const file = try std.fs.cwd().openFile("../../src/enum_negative.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_bytes_non_literal.ExprBytesNonLiteral.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, (try r.calcBytes()).len);
    try std.testing.expectEqual(255, (try r.calcBytes())[0]);
    try std.testing.expectEqual(1, (try r.calcBytes())[1]);
}
