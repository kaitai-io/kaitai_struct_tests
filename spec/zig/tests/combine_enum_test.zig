// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const combine_enum = @import("../formats/combine_enum.zig");

test "CombineEnum" {
    const file = try std.fs.cwd().openFile("../../src/enum_0.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try combine_enum.CombineEnum.create(&arena, &_io, null, null);
    try std.testing.expectEqual(combine_enum.CombineEnum.Animal.pig, r.enum_u4);
    try std.testing.expectEqual(combine_enum.CombineEnum.Animal.horse, r.enum_u2);
    try std.testing.expectEqual(combine_enum.CombineEnum.Animal.horse, (try r.enumU4U2()));
}
