// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const debug_array_user_current_excluded = @import("../formats/debug_array_user_current_excluded.zig");

test "DebugArrayUserCurrentExcluded" {
    const file = try std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try debug_array_user_current_excluded.DebugArrayUserCurrentExcluded.create(&arena, &_io, null, null);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 102, 111, 111 }, r.array_of_cats.items[0].meow);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 124, 98 }, r.array_of_cats.items[1].meow);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 97 }, r.array_of_cats.items[2].meow);
}
