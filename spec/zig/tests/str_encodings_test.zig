// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const str_encodings = @import("../formats/str_encodings.zig");

test "StrEncodings" {
    const file = try std.fs.cwd().openFile("../../src/str_encodings.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try str_encodings.StrEncodings.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("Some ASCII", r.str1);
    try std.testing.expectEqualStrings("\u{3053}\u{3093}\u{306b}\u{3061}\u{306f}", r.str2);
    try std.testing.expectEqualStrings("\u{3053}\u{3093}\u{306b}\u{3061}\u{306f}", r.str3);
    try std.testing.expectEqualStrings("\u{2591}\u{2592}\u{2593}", r.str4);
}
