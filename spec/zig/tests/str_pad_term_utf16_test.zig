// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const str_pad_term_utf16 = @import("../formats/str_pad_term_utf16.zig");

test "StrPadTermUtf16" {
    const file = try std.fs.cwd().openFile("../../src/str_pad_term_utf16.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try str_pad_term_utf16.StrPadTermUtf16.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("a\u{0200}b", r.str_term);
    try std.testing.expectEqualStrings("c\u{0200}d\u{0000}", r.str_term_include);
    try std.testing.expectEqualStrings("e\u{0200}f", r.str_term_and_pad);
}
