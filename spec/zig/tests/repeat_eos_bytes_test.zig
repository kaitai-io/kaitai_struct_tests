// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_repeat_eos_bytes = @import("../formats/repeat_eos_bytes.zig");

test "RepeatEosBytes" {
    const file = try _imp_std.fs.cwd().openFile("../../src/repeat_until_process.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_repeat_eos_bytes.RepeatEosBytes.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(3, r.records.items.len);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 232, 186, 170, 170, 170 }, r.records.items[0]);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 250, 158, 184, 170, 170 }, r.records.items[1]);
    try _imp_std.testing.expectEqualSlices(u8, &[_]u8{ 170, 85, 85, 85, 85 }, r.records.items[2]);
}
