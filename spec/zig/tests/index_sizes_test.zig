// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const index_sizes = @import("../formats/index_sizes.zig");

test "IndexSizes" {
    const file = try std.fs.cwd().openFile("../../src/index_sizes.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try index_sizes.IndexSizes.create(&arena, &_io, null, null);
    try std.testing.expectEqual(3, r.qty);
    try std.testing.expectEqual(1, r.sizes.items[0]);
    try std.testing.expectEqual(8, r.sizes.items[1]);
    try std.testing.expectEqual(4, r.sizes.items[2]);
    try std.testing.expectEqualStrings("A", r.bufs.items[0]);
    try std.testing.expectEqualStrings("BBBBBBBB", r.bufs.items[1]);
    try std.testing.expectEqualStrings("CCCC", r.bufs.items[2]);
}
