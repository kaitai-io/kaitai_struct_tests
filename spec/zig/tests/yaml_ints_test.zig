// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const yaml_ints = @import("../formats/yaml_ints.zig");

test "YamlInts" {
    const file = try std.fs.cwd().openFile("../../src/fixed_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try yaml_ints.YamlInts.create(&arena, &_io, null, null);
    try std.testing.expectEqual(4294967295, (try r.testU4Dec()));
    try std.testing.expectEqual(4294967295, (try r.testU4Hex()));
    try std.testing.expectEqual(18446744073709551615, (try r.testU8Dec()));
    try std.testing.expectEqual(18446744073709551615, (try r.testU8Hex()));
}
