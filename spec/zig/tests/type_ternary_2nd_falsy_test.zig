// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_type_ternary_2nd_falsy = @import("../formats/type_ternary_2nd_falsy.zig");

test "TypeTernary2ndFalsy" {
    const file = try _imp_std.fs.cwd().openFile("../../src/switch_integers.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_type_ternary_2nd_falsy.TypeTernary2ndFalsy.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(false, (try r.vFalse()));
    try _imp_std.testing.expectEqual(0, (try r.vIntZero()));
    try _imp_std.testing.expectEqual(0, (try r.vIntNegZero()));
    try _imp_std.testing.expectEqual(0.0, (try r.vFloatZero()));
    try _imp_std.testing.expectEqual(-0.0, (try r.vFloatNegZero()));
    try _imp_std.testing.expectEqualStrings("0", (try r.vStrWZero()));
    try _imp_std.testing.expectEqual(1, (try r.vStrWZero()).len);
    try _imp_std.testing.expectEqual(7, r.ut.m);
    try _imp_std.testing.expectEqual(null, (try r.vNullUt()));
    try _imp_std.testing.expectEqualStrings("", (try r.vStrEmpty()));
    try _imp_std.testing.expectEqual(0, (try r.vStrEmpty()).len);
    try _imp_std.testing.expectEqual(2, r.int_array.items.len);
    try _imp_std.testing.expectEqual(0, (try r.vIntArrayEmpty()).items.len);
}
