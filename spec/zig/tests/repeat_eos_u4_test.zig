// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const repeat_eos_u4 = @import("../formats/repeat_eos_u4.zig");

test "RepeatEosU4" {
    const file = try std.fs.cwd().openFile("../../src/repeat_eos_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try repeat_eos_u4.RepeatEosU4.create(&arena, &_io, null, null);
    try std.testing.expectEqual(4, r.numbers.items.len);
    try std.testing.expectEqual(0, r.numbers.items[0]);
    try std.testing.expectEqual(66, r.numbers.items[1]);
    try std.testing.expectEqual(66, r.numbers.items[2]);
    try std.testing.expectEqual(2069, r.numbers.items[3]);
}
