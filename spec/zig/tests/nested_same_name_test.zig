// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nested_same_name = @import("../formats/nested_same_name.zig");

test "NestedSameName" {
    const file = try std.fs.cwd().openFile("../../src/repeat_n_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nested_same_name.NestedSameName.create(&arena, &_io, null, null);
    try std.testing.expectEqual(2, r.main_data.main_size);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 16, 0, 0, 0 }, r.main_data.foo.data);
}
