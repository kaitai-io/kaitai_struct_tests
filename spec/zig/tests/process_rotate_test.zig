// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const process_rotate = @import("../formats/process_rotate.zig");

test "ProcessRotate" {
    const file = try std.fs.cwd().openFile("../../src/process_rotate.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try process_rotate.ProcessRotate.create(&arena, &_io, null, null);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 72, 101, 108, 108, 111 }, r.buf1);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 87, 111, 114, 108, 100 }, r.buf2);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 84, 104, 101, 114, 101 }, r.buf3);
}
