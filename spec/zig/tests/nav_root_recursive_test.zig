// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nav_root_recursive = @import("../formats/nav_root_recursive.zig");

test "NavRootRecursive" {
    const file = try std.fs.cwd().openFile("../../src/enum_negative.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nav_root_recursive.NavRootRecursive.create(&arena, &_io, null, null);
    try std.testing.expectEqual(255, r.value);
    try std.testing.expectEqual(1, r.next.value);
    try std.testing.expectEqual(255, (try r.next.rootValue()));
    try std.testing.expectEqual(null, r.next.next);
}
