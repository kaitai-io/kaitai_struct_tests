// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const bcd_user_type_be = @import("../formats/bcd_user_type_be.zig");

test "BcdUserTypeBe" {
    const file = try std.fs.cwd().openFile("../../src/bcd_user_type_be.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try bcd_user_type_be.BcdUserTypeBe.create(&arena, &_io, null, null);
    try std.testing.expectEqual(12345678, (try r.ltr.asInt()));
    try std.testing.expectEqualStrings("12345678", (try r.ltr.asStr()));
    try std.testing.expectEqual(87654321, (try r.rtl.asInt()));
    try std.testing.expectEqualStrings("87654321", (try r.rtl.asStr()));
    try std.testing.expectEqual(123456, (try r.leading_zero_ltr.asInt()));
    try std.testing.expectEqualStrings("00123456", (try r.leading_zero_ltr.asStr()));
}
