// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const nav_parent_vs_value_inst = @import("../formats/nav_parent_vs_value_inst.zig");

test "NavParentVsValueInst" {
    const file = try std.fs.cwd().openFile("../../src/term_strz.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try nav_parent_vs_value_inst.NavParentVsValueInst.create(&arena, &_io, null, null);
    try std.testing.expectEqualStrings("foo", r.s1);
}
