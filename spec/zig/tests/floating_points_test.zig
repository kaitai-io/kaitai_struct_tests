// Autogenerated from KST: please remove this line if doing any edits by hand!

const _imp_std = @import("std");
const _imp_kaitai_struct = @import("kaitai_struct");
const _imp_floating_points = @import("../formats/floating_points.zig");

test "FloatingPoints" {
    const file = try _imp_std.fs.cwd().openFile("../../src/floating_points.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = _imp_std.testing.allocator;
    var arena = _imp_std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = _imp_kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try _imp_floating_points.FloatingPoints.create(&arena, &_io, null, null);
    try _imp_std.testing.expectEqual(@as(f32, 0.5), r.single_value);
    try _imp_std.testing.expectEqual(@as(f32, 0.5), r.single_value_be);
    try _imp_std.testing.expectEqual(0.25, r.double_value);
    try _imp_std.testing.expectEqual(0.25, r.double_value_be);
    try _imp_std.testing.expectEqual(1.2345, r.approximate_value);
    try _imp_std.testing.expectEqual(1.5, (try r.singleValuePlusInt()));
    try _imp_std.testing.expectEqual(1.0, (try r.singleValuePlusFloat()));
    try _imp_std.testing.expectEqual(0.3, (try r.doubleValuePlusFloat()));
}
