// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_array = @import("../formats/expr_array.zig");

test "ExprArray" {
    const file = try std.fs.cwd().openFile("../../src/expr_array.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_array.ExprArray.create(&arena, &_io, null, null);
    try std.testing.expectEqual(4, (try r.aintSize()));
    try std.testing.expectEqual(7657765, (try r.aintFirst()));
    try std.testing.expectEqual(16272640, (try r.aintLast()));
    try std.testing.expectEqual(49185, (try r.aintMin()));
    try std.testing.expectEqual(1123362332, (try r.aintMax()));
    try std.testing.expectEqual(3, (try r.afloatSize()));
    try std.testing.expectEqual(-2.6839530254859364E-121, (try r.afloatFirst()));
    try std.testing.expectEqual(-1.1103359815095273E-175, (try r.afloatLast()));
    try std.testing.expectEqual(-8.754689149998834E+288, (try r.afloatMin()));
    try std.testing.expectEqual(-1.1103359815095273E-175, (try r.afloatMax()));
    try std.testing.expectEqual(3, (try r.astrSize()));
    try std.testing.expectEqualStrings("foo", (try r.astrFirst()));
    try std.testing.expectEqualStrings("baz", (try r.astrLast()));
    try std.testing.expectEqualStrings("bar", (try r.astrMin()));
    try std.testing.expectEqualStrings("foo", (try r.astrMax()));
}
