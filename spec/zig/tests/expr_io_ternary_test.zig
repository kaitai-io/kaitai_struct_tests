// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_io_ternary = @import("../formats/expr_io_ternary.zig");

test "ExprIoTernary" {
    const file = try std.fs.cwd().openFile("../../src/if_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_io_ternary.ExprIoTernary.create(&arena, &_io, null, null);
    try std.testing.expectEqual(8, (try r.oneOrTwoIoSize1()));
    try std.testing.expectEqual(8, (try r.oneOrTwoIoSize2()));
    try std.testing.expectEqual(11, (try r.oneOrTwoIoSizeAdd3()));
}
