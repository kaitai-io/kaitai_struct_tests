// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_io_eof_bits = @import("../formats/expr_io_eof_bits.zig");

test "ExprIoEofBits" {
    const file = try std.fs.cwd().openFile("../../src/nav_parent_switch.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_io_eof_bits.ExprIoEofBits.create(&arena, &_io, null, null);
    try std.testing.expectEqual(5167, r.foo);
    try std.testing.expectEqual(15, r.bar);
    try std.testing.expectEqual(null, r.baz);
    try std.testing.expectEqualSlices(u8, &[_]u8{  }, r.align);
    try std.testing.expectEqual(null, r.qux);
}
