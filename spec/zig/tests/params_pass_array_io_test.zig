// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const params_pass_array_io = @import("../formats/params_pass_array_io.zig");

test "ParamsPassArrayIo" {
    const file = try std.fs.cwd().openFile("../../src/enum_negative.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try params_pass_array_io.ParamsPassArrayIo.create(&arena, &_io, null, null);
    try std.testing.expectEqual(255, r.first.foo);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 1 }, r.one.buf);
}
