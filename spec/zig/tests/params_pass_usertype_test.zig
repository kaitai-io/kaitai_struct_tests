// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const params_pass_usertype = @import("../formats/params_pass_usertype.zig");

test "ParamsPassUsertype" {
    const file = try std.fs.cwd().openFile("../../src/position_in_seq.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try params_pass_usertype.ParamsPassUsertype.create(&arena, &_io, null, null);
    try std.testing.expectEqual(1, r.first.foo);
    try std.testing.expectEqualSlices(u8, &[_]u8{ 2 }, r.one.buf);
}
