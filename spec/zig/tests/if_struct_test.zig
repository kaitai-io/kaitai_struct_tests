// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const if_struct = @import("../formats/if_struct.zig");

test "IfStruct" {
    const file = try std.fs.cwd().openFile("../../src/if_struct.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try if_struct.IfStruct.create(&arena, &_io, null, null);
    try std.testing.expectEqual(83, r.op1.opcode);
    try std.testing.expectEqual(null, r.op1.arg_tuple);
    try std.testing.expectEqualStrings("foo", r.op1.arg_str.str);
    try std.testing.expectEqual(84, r.op2.opcode);
    try std.testing.expectEqual(66, r.op2.arg_tuple.num1);
    try std.testing.expectEqual(67, r.op2.arg_tuple.num2);
    try std.testing.expectEqual(null, r.op2.arg_str);
    try std.testing.expectEqual(83, r.op3.opcode);
    try std.testing.expectEqual(null, r.op3.arg_tuple);
    try std.testing.expectEqualStrings("bar", r.op3.arg_str.str);
}
