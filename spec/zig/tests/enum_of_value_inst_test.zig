// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const enum_of_value_inst = @import("../formats/enum_of_value_inst.zig");

test "EnumOfValueInst" {
    const file = try std.fs.cwd().openFile("../../src/enum_0.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try enum_of_value_inst.EnumOfValueInst.create(&arena, &_io, null, null);
    try std.testing.expectEqual(enum_of_value_inst.EnumOfValueInst.Animal.cat, r.pet_1);
    try std.testing.expectEqual(enum_of_value_inst.EnumOfValueInst.Animal.chicken, r.pet_2);
    try std.testing.expectEqual(enum_of_value_inst.EnumOfValueInst.Animal.dog, (try r.pet3()));
    try std.testing.expectEqual(enum_of_value_inst.EnumOfValueInst.Animal.dog, (try r.pet4()));
}
