// Autogenerated from KST: please remove this line if doing any edits by hand!

const std = @import("std");
const kaitai_struct = @import("kaitai_struct");
const expr_str_encodings = @import("../formats/expr_str_encodings.zig");

test "ExprStrEncodings" {
    const file = try std.fs.cwd().openFile("../../src/str_encodings.bin", .{});
    defer file.close();
    var buffer: [8]u8 = undefined;
    var reader = file.reader(&buffer);
    const allocator = std.testing.allocator;
    var arena = std.heap.ArenaAllocator.init(allocator);
    defer arena.deinit();
    var _io = kaitai_struct.KaitaiStream.fromFileReader(&reader);
    const r = try expr_str_encodings.ExprStrEncodings.create(&arena, &_io, null, null);
    try std.testing.expectEqual(true, (try r.str1Eq()));
    try std.testing.expectEqual(true, (try r.str2Eq()));
    try std.testing.expectEqual(true, (try r.str3Eq()));
    try std.testing.expectEqual(true, (try r.str3EqStr2()));
    try std.testing.expectEqual(true, (try r.str4Eq()));
    try std.testing.expectEqual(true, (try r.str4GtStrCalc()));
    try std.testing.expectEqual(true, (try r.str4GtStrFromBytes()));
}
