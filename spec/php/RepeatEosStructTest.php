<?php
namespace Kaitai\Struct\Tests;

class RepeatEosStructTest extends \PHPUnit_Framework_TestCase {
    public function testRepeatEosStruct() {
        $this->markTestIncomplete();
/*
        RepeatEosStruct r = RepeatEosStruct::fromFile(self::SRC_DIR_PATH . "repeat_eos_struct.bin");

        $this->assertEquals(r.chunks().size(), 2);
        $this->assertEquals(r.chunks().get(0).offset(), 0);
        $this->assertEquals(r.chunks().get(0).len(), 0x42);
        $this->assertEquals(r.chunks().get(1).offset(), 0x42);
        $this->assertEquals(r.chunks().get(1).len(), 0x815);
*/    
    }
}
