<?php
// Autogenerated from KST: please remove this line if doing any edits by hand!

namespace Kaitai\Struct\Tests;

class RepeatUntilComplexTest extends TestCase {
    public function testRepeatUntilComplex() {
        $r = RepeatUntilComplex::fromFile(self::SRC_DIR_PATH . '/repeat_until_complex.bin');

        $this->assertSame(3, count($r->first()));
        $this->assertSame(4, $r->first()[0]->count());
        $this->assertSame([(0 + 1), 2, 3, 4], $r->first()[0]->values());
        $this->assertSame(2, $r->first()[1]->count());
        $this->assertSame([(0 + 1), 2], $r->first()[1]->values());
        $this->assertSame(0, $r->first()[2]->count());
        $this->assertSame(4, count($r->second()));
        $this->assertSame(6, $r->second()[0]->count());
        $this->assertSame([(0 + 1), 2, 3, 4, 5, 6], $r->second()[0]->values());
        $this->assertSame(3, $r->second()[1]->count());
        $this->assertSame([(0 + 1), 2, 3], $r->second()[1]->values());
        $this->assertSame(4, $r->second()[2]->count());
        $this->assertSame([(0 + 1), 2, 3, 4], $r->second()[2]->values());
        $this->assertSame(0, $r->second()[3]->count());
        $this->assertSame([(0 + 102), 111, 111, 98, 97, 114, 0], $r->third());
    }
}
